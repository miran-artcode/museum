# 최종 작품 상호평가 설계: 쌍대비교와 세 번의 평가

8차시의 상호평가를 **최종 작품 제출 → 자기평가 ① → 동료 쌍대비교 → 자기평가 ②** 로 다시 짠 설계서입니다.
구현 전에 큰 틀을 확정하기 위한 문서이며, 화면·데이터·알고리즘·수업 운영·연구 지표를 함께 담았습니다.

---

## 0. 한눈에 보는 틀

```
[교사]  제출 열기 ─→ 비교 명단 확정 ─→ 비교 열기 ─→ 집계 실행 ─→ 결과 공개
           │              │                │            │             │
[학생]  ① 최종 작품 제출   ② 자기평가 ①     ③ 쌍대비교 k쌍   │        ⑤ 자기평가 ②
        (스냅샷 잠금)     (4축 + 등수 예측) (승자 + 이유)   └→ 내 결과  (①과 나란히, 변화 서술)
```

- 평가의 단위는 **점수가 아니라 비교**입니다. 학생은 “이 작품 몇 점?”이 아니라 “둘 중 어느 쪽이 더 성립하는가”에 답합니다.
- 자기평가는 같은 4축으로 **두 번** 하고, 그 사이에 동료 비교가 들어갑니다. 두 자기평가의 차이가 이 설계의 핵심 자료입니다.

### “자기평가–동료평가–자기평가 3번”의 처리

기본값은 **한 묶음 3단계(자기 ① · 비교 · 자기 ②)**입니다. 다만 데이터 구조는 단계를 배열로 두어
`["self","peer","self"]`를 `["self","peer","self","peer","self"]`처럼 늘리면 그대로 여러 회차가 돌아갑니다.
단계마다 고유 id(`s1`·`p1`·`s2`…)로 저장하므로 회차를 늘려도 앞의 자료가 어긋나지 않습니다.
수업 시간으로는 3단계 한 묶음이 8차시 100분 안에 들어가고, 세 묶음을 다 돌리려면 두 차시가 더 필요합니다.

---

## 1. 왜 쌍대비교인가

- **절대 평정의 한계.** 고1이 “독창성 5점 만점에 3점”을 일관되게 매기기는 어렵습니다. 기준점이 사람마다 다르고, 친분이 점수에 그대로 실립니다.
- **비교는 쉽다.** 서스톤(Thurstone, 1927)의 비교판단 법칙 이래, 사람은 절대 평정보다 두 대상의 우열 판단을 훨씬 안정적으로 합니다. 폴릿(Pollitt)의 적응적 비교판단(ACJ)은 이 원리를 채점에 옮긴 것으로, 개별 판단이 거칠어도 여러 사람의 판단을 모으면 전체 순위가 신뢰도 있게 복원됩니다.
- **판단의 근거가 글로 남는다.** 쌍마다 이유 한 문장을 강제하면, 점수표로는 남지 않는 “무엇을 보고 그렇게 판단했는가”가 판정 수만큼 쌓입니다. 이 문장들이 8차시 `s8a.peerBack`(받은 문장의 반영 여부)의 재료가 됩니다.
- **자기평가의 보정.** 동료 작품 스무 점 남짓을 근거를 대며 비교한 직후에 자기 작품을 다시 평가하면, 기준이 학급의 실제 분포에 맞춰집니다. 이 보정의 크기(자기평가 ①과 ②의 차이)를 재는 것이 이 설계의 연구 목표입니다.

기존 연구 설계(little-c, 단일집단 사전·사후, 삼각검증)와 이어집니다. 자기보고(설문) · 행동지표(창의성 6축) · 질적 기록에 **동료 판단**이라는 네 번째 축이 붙습니다.

---

## 2. 세 단계에서 학생이 하는 일

| 단계 | 화면 | 학생이 하는 일 | 저장 위치 | 잠금 |
|---|---|---|---|---|
| 제출 | 최종 작품 제출 | 대표 이미지·제목·작품 캡션 6줄·작가 노트를 확인하고 **제출 확정** | `submissions/{학번}` | 확정 시 잠김, 교사만 해제 |
| 자기평가 ① | 자기평가 | 4축 1~5점 + 축마다 근거 한 줄 + **우리 반에서 몇 등쯤일지 예측** | `assess/{학번}.self.s1` | 제출 시 잠김 |
| 동료 비교 | 쌍대비교 | k쌍(기본 12쌍) 승자 선택 + **이유 한 문장(필수)** + 결정적 축 태그 | `assess/{학번}.judge.p1` | 쌍 단위 저장, 되돌리기 1회 |
| 자기평가 ② | 자기평가 | 같은 4축 재평정(①의 답을 나란히 표시) + 등수 재예측 + **무엇이 판단을 바꿨는지** + 받은 문장 읽고 반영 여부 | `assess/{학번}.self.s2` | 제출 시 잠김 |
| 결과 | 내 결과 | 내 작품의 위치(밴드/백분위), 받은 이유 문장(익명), 내 예측과의 차이 | `grades/{학번}.peer` (본인만 읽음) | 교사가 공개할 때만 |

### 평가 4축 (자기평가·이유 태그 공용)

7차시까지 쓰던 말(`PEER_VIEWS`, `INSPECT_ITEMS`)을 네 개로 줄여 씁니다.

| 축 | 묻는 것 |
|---|---|
| `veri` 이미지의 핍진성 | 유물 기록 사진의 형식(배경·조명·스케일)이 성립하는가 |
| `cause` 흔적의 인과 | 닳고 부서지고 수리된 자리가 그 물건의 쓰임과 이어지는가 |
| `plate` 작품 캡션의 작동 | 작품 캡션을 읽은 뒤 이미지의 읽기가 실제로 달라지는가 |
| `voice` 문제의 전달 | 어떤 사회문제가 어떤 태도(고발·경고·공감·기록·질문)로 읽히는가 |

### 쌍대비교의 질문은 하나

비교 화면에서는 축별로 묻지 않고 **종합 질문 하나**만 던집니다.

> 두 작품 가운데 **실재한 적 없는 유물을 더 설득력 있게 성립시킨 쪽**은 어디인가.
> (이미지 · 작품 캡션 · 허구 고지가 함께 작동해서)

축을 쌍마다 네다섯 번 물으면 판정 시간이 그만큼 늘고 피로가 신뢰도를 깎습니다.
대신 승자를 고른 뒤 **결정적이었던 축 하나를 태그**로 고르게 해, 축 정보는 잃지 않으면서 판정은 한 번에 끝냅니다.
축 목록과 종합 질문 문구는 `config.peer.criteria` / `config.peer.question`으로 교사가 고칠 수 있게 둡니다.

---

## 3. 화면 설계

학생 화면 차시 탭(1~8차시) 오른쪽에 **「최종 평가」 탭**을 하나 붙입니다. `config.peer.stage`가 `closed`가 아닐 때만 나타납니다.
탭 안의 화면은 단계에 따라 바뀌므로 학생은 “지금 할 것” 하나만 봅니다.

```
┌ 최종 평가 ────────────────────────────────────────────┐
│  ● 제출 ── ○ 자기평가① ── ○ 동료 비교 ── ○ 자기평가② ── ○ 결과  │ ← 진행 띠
└──────────────────────────────────────────────────┘
```

### 3.1 제출 화면

```
작품 번호 [A-03]        (비었으면 학번 순으로 자동 제안)
대표 이미지 [ 이미지 ]  ← 7차시 s7x.img가 있으면 자동으로 끌어옴
제목      「오른손」     ← s6b.title
작품 캡션 6줄              ← s6b.* 자동 채움, 이 화면에서 고칠 수 있음
작가 노트               ← s7.note
──────────────────────────────────────────────
[ 제출 확정 ]  누르면 위 내용이 그대로 굳고 비교 대상이 됩니다. 고치려면 선생님께 해제를 요청하세요.
```

- **스냅샷으로 굳히는 이유**: 비교가 도는 중에 작가가 원본 기록지를 고치면, 먼저 판정한 학생과 나중에 판정한 학생이 서로 다른 작품을 본 것이 됩니다. 비교 자료의 동일성을 지키려면 제출 시점의 사본이 따로 있어야 합니다.
- 제출하지 않은 학생도 **판정에는 참여**합니다(자기 작품만 비교 대상에서 빠짐). 판정 경험 자체가 수업 목표이기 때문입니다.

### 3.2 자기평가 화면 (①·② 공용)

```
[veri]  이미지의 핍진성   ①②③④⑤   근거 한 줄 [                     ]
[cause] 흔적의 인과       ①②③④⑤   근거 한 줄 [                     ]
[plate] 작품 캡션의 작동     ①②③④⑤   근거 한 줄 [                     ]
[voice] 문제의 전달       ①②③④⑤   근거 한 줄 [                     ]

우리 반 24점 가운데 내 작품은 몇 번째쯤일까요?
 1등 ├────────────●──────────┤ 24등     (등수와 백분위를 함께 저장)
```

자기평가 ②에서는 각 줄 옆에 **①에서 내가 준 점수와 근거**가 회색으로 함께 보입니다(기존 `reflect` 필드의 `showPrev` 패턴 재사용).
아래에 필수 서술 한 칸을 둡니다. “비교하면서 본 것 가운데 내 작품을 다시 보게 만든 장면은 무엇이었나. 어느 작품의 무엇이었나.”

### 3.3 쌍대비교 화면

```
        3 / 12 쌍                                    [ 작품 캡션 보기 ▸ ]

┌──── 작품 A-07 ────┐    ┌──── 작품 B-02 ────┐
│     [ 이미지 ]     │    │     [ 이미지 ]     │
│  「제목」 (펼치면 공개) │    │  「제목」 (펼치면 공개) │
│   작품 캡션 (접힘)     │    │   작품 캡션 (접힘)     │
└──────────────────┘    └──────────────────┘
         [ 왼쪽 ]                  [ 오른쪽 ]

고른 이유를 한 문장으로 (필수, 15자 이상)
[                                                        ]
결정적이었던 것:  ( ) 핍진성  ( ) 흔적의 인과  ( ) 작품 캡션  ( ) 문제의 전달

[ ← 직전 판정 고치기 ]                            [ 다음 쌍 → ]
```

- 작품 캡션은 **접힌 채로 시작**합니다. 전시장의 1차·2차 관람 규칙을 비교 화면에서도 지키고, 무엇을 보고 판단했는지(이미지만인가, 작품 캡션까지인가)를 `plateOpened`로 기록합니다.
- 좌우 위치는 회차마다 뒤집어 **위치 편향**을 없앱니다.
- 판정 소요 시간(ms)을 쌍마다 기록합니다. 5초 미만이면 “한 번 더 보고 고르세요” 안내가 뜨고 넘어가지 않습니다.
- 자기 작품은 절대 나오지 않습니다.

### 3.4 교사 화면: 새 탭 「상호평가」

```
단계  [ 닫힘 | 제출 | 자기평가① | 동료 비교 | 자기평가② | 결과 공개 ]   ← 라디오 하나로 전 학급 제어
제출 18/24  ·  명단 확정 3/12 14:20 (18점)  ·  비교 완료 11/24 (판정 152건)

[ 비교 명단 확정 ]   [ 집계 실행 ]   [ 결과 공개 ]

순위표                                    신뢰도·품질
번호   점수   승/패  노출  자기예측  차이     반분 신뢰도 .83 (판정 152건, 작품당 평균 16.9회)
A-07   1.42   14/3   17    3등    +2      판정자 일관성 하위: 21103 · 21118 · 21107
B-02   0.91   12/5   17   11등    -8      판정 중앙시간 34초 · 5초 미만 판정 2건
 …

[CSV]  제출 · 판정 · 자기평가 · 집계
```

자기평가와 동료평가의 격차는 기존 `ApproprScatter`와 같은 산점도로 봅니다(가로: 실제 백분위, 세로: 예측 백분위, 대각선에서 멀수록 오차).

---

## 4. 데이터 모델

기존 규약(`{ v: 값, updatedAt }` 문서, 학번이 문서 id, `store.get/set` 키 라우팅)을 그대로 따릅니다.
`src-fb.js`의 `route()`에 `sub:` 와 `assess:` 두 줄을 더합니다.

### 4.1 `submissions/{학번}`: 학급 전체가 읽는 작품 스냅샷

```js
{ v: {
  no: "A-03",                       // 작품 번호 (비교 명단의 정렬 키)
  title: "오른손",
  plate: { relic, year, era, mat, size, context, coll, aiScope, notice },
  note: "작가 노트…",
  img: { ref: "s7x.img.1712…" },    // media/{학번}_… 를 가리킴 (기존 미디어 계층 그대로)
  submittedAt: "2026-…", ver: "v1", locked: true
} }
```

### 4.2 `assess/{학번}`: 본인과 교사만 읽는 평가 기록

```js
{ v: {
  ver: "v1",
  self: {
    s1: { scores: { veri: 4, cause: 3, plate: 5, voice: 3 },
          why:    { veri: "…", cause: "…" },
          predRank: 8, predPct: 0.33, startedAt, submittedAt, durSec },
    s2: { …같은 구조…, changed: "비교하면서 …", cite: "A-11의 작품 캡션" }
  },
  judge: {
    p1: { rosterVer: "2026-04-02T05:20Z", k: 12,
          items: [ { i: 0, a: "A-07", b: "B-02", win: "a",
                     why: "…", tag: "cause", ms: 41200, plateOpened: true, at: "…" }, … ],
          startedAt, submittedAt }
  }
} }
```

12판정이 4KB 남짓이므로 문서 하나로 충분합니다(규칙의 1MB 상한 안).

### 4.3 `meta/peerRoster`: 비교 명단 (교사만 쓰기, 로그인하면 읽기)

```js
{ v: { fixedAt: "2026-04-02T05:20Z", seed: 20260402, k: 12,
       works: ["A-01", "A-03", "A-07", "B-02", …] } }   // 오름차순, 색인 = 배열 위치
```

명단을 **문서로 굳히는 것이 이 설계에서 가장 중요한 한 줄**입니다. 명단이 실시간으로 바뀌면 학생마다 다른 쌍을 보게 되어 노출 균형이 깨지고 점수를 견줄 수 없게 됩니다. 소유자 정보는 이 문서에 넣지 않습니다.

### 4.4 `config.peer`: 단계 스위치

```js
peer: { stage: "closed" | "submit" | "self1" | "peer" | "self2" | "result",
        k: 12, minSec: 5, minWhy: 15,
        reveal: "band",                 // band(상·중·하) | pct | rank | none
        criteria: [ … ], question: "…", stages: ["self", "peer", "self"] }
```

기본값 `{ stage: "closed" }`. 이 기능을 켜기 전까지 기존 학급 화면은 아무것도 달라지지 않습니다.

### 4.5 결과 되돌려주기

집계 결과는 학생별로 **`grades/{학번}.peer`**에 씁니다. 이 컬렉션은 이미 “본인 읽기 · 교사 쓰기” 규칙이라, 학급 순위표 전체를 노출하지 않고도 학생이 자기 결과만 봅니다. 새 규칙이 필요 없습니다.

```js
peer: { score: 1.42, pct: 0.87, band: "상", plays: 17, wins: 14,
        predPct1: 0.33, predPct2: 0.72,                    // 자기예측 ①②
        received: ["흔적이 손잡이 안쪽에만 있어서…", …],      // 나를 두고 쓴 이유 문장(익명)
        aggAt: "…" }
```

### 4.6 보안 규칙 추가분

```
match /submissions/{sid} {
  allow read: if signedIn();                                          // 비교·전시를 위해 학급 공개
  allow create, update: if (isOwner(sid) || isTeacher()) && sizeOk();
  allow delete: if isTeacher();
}
match /assess/{sid} {
  allow read: if isOwner(sid) || isTeacher();
  allow create, update: if (isOwner(sid) || isTeacher()) && sizeOk();  // 교사 쓰기는 잠금 해제용
  allow delete: if isTeacher();
}
```

**익명성의 한계, 솔직하게 적어 둡니다.** 화면에는 작품 번호만 뜨지만, `submissions` 문서 id가 학번이고 이미지가 `media/{학번}_…`에 있어 개발자 도구를 열면 소유자를 알 수 있습니다. 완전 익명은 서버(Cloud Functions)에서 소유자를 지운 사본을 만들어야 가능하며, 지금 구조(클라이언트 + 보안 규칙)에서는 **표시상 익명 · 저장상 준익명**입니다. 학급 규범으로 다루고, 필요하면 후속 과제로 남깁니다.

**연구 동의와의 연결.** 최근에 들어온 `research/` 동의 대장(교사만 읽고 씀)을 그대로 씁니다. 판정·자기평가 자료는 수업 활동이므로 전원이 하되, **CSV 내보내기와 논문용 집계에서는 동의하지 않은 학생을 제외**합니다.

---

## 5. 누가 무엇과 비교하는가: 쌍 만들기

서버 없이도 학급 전체의 노출이 고르게 되도록, **모든 학생이 같은 규칙으로 자기 쌍 목록을 계산**합니다.
명단의 작품 수를 n, 판정자의 색인을 j(미제출 판정자는 명단 뒤의 가상 색인), 회차를 r = 1…k라 할 때

```
A = (j + r) mod n
B = (j + r + d_r) mod n,   d_r = 1 + ((r × 7) mod (n − 2))
   ※ B가 A 또는 j와 같아지면 d_r을 1씩 늘려 피한다
   ※ 좌우 표시는 (j + r)이 홀수일 때 뒤집는다
```

이 규칙의 성질:

- **자기 작품 제외.** r ≥ 1이고 k < n이므로 A ≠ j, B는 충돌 회피로 j를 피합니다.
- **노출 균형.** 회차 r을 고정하면 j가 학급을 한 바퀴 도는 동안 A도 모든 작품을 정확히 한 번씩 덮습니다. B도 마찬가지입니다. 따라서 모두가 k회차를 마치면 **작품마다 정확히 2k회** 노출됩니다(24명·k=12 → 작품당 24회, 총 288판정).
- **재접속 안전.** 쌍 목록은 저장된 값이 아니라 (명단, 학번, 회차)로 매번 계산되므로 앱을 껐다 켜도 같은 순서가 나옵니다. 이어서 할 지점은 `items.length`로 정합니다.

**k(1인당 판정 수)의 기준.** 비교판단 문헌에서는 작품당 열 번 안팎의 비교면 실용적인 신뢰도(반분 신뢰도 .8 내외)에 이른다고 봅니다. 여기서는 1인당 k쌍이 작품당 2k회 노출로 돌아오므로 **k = 8~12면 작품당 16~24회**로 넉넉합니다. 기본값 `k = min(12, n−1)`, 하한 6.

**적응적 비교(ACJ)는 넣지 않습니다.** 점수가 가까운 작품끼리 붙이면 정보량이 늘지만, 그러려면 학생 화면이 다른 학생의 판정을 실시간으로 읽어야 해서 규칙을 크게 열어야 합니다. 학급 규모(20~30명)에서는 균형 배정만으로 충분합니다.

**예외 처리**

| 상황 | 처리 |
|---|---|
| n < 4 | 비교 불가 안내, 자기평가만 진행 |
| 마감 뒤 늦은 제출 | 판정에는 참여, 자기 작품은 다음 명단 확정 때 편입(기록에 남김) |
| 결석·중도 이탈 | 판정 수가 적어도 점수는 산출(노출 불균형은 집계에서 보정), 교사 화면에 미완료자 표시 |
| 명단 재확정 | `rosterVer`가 바뀌면 이전 판정은 그대로 두고 새 회차(`p2`)로 쌓음 |

---

## 6. 점수 산출과 신뢰도

### 6.1 작품 점수: 브래들리–테리

승률만 쓰면 “센 상대와 붙어 진 작품”이 부당하게 밀립니다. 브래들리–테리 모형을 MM 반복(50회)으로 풀어 능력값을 구하고, z점수 → 백분위로 바꿉니다.

```
p_i ← w_i / Σ_j ( n_ij / (p_i + p_j) )   를 수렴할 때까지, 매 반복 정규화
θ_i = ln p_i
```

전승·전패 작품에서 값이 발산하지 않도록 **가상의 평균 작품과 0.5승 0.5패**를 미리 넣습니다(정규화 사전).
교사 화면에는 θ 옆에 승률과 노출 수를 함께 보여 줘, 모형을 몰라도 읽을 수 있게 합니다.

### 6.2 신뢰도: 반분(split-half)

판정을 홀수 번째·짝수 번째로 나눠 각각 브래들리–테리로 풀고, 두 점수의 상관 r을 스피어만–브라운으로 보정합니다(`2r / (1+r)`).
이 값이 **.7 아래면 결과 공개를 보류**하고 판정을 더 받도록 교사 화면에 안내를 띄웁니다.

### 6.3 판정자 일관성

각 판정자에 대해, 최종 점수 차가 큰(θ 차 ≥ 0.5) 쌍에서 반대로 판정한 비율을 셉니다. 이것은 **성적이 아니라 수업 자료**입니다.
일관성이 낮은 판정자가 어떤 축을 태그했는지 보면 기준이 어긋난 지점이 드러나고, 그것이 8차시 토의의 좋은 소재가 됩니다.

### 6.4 성실하지 않은 판정 거르기

- 이유 15자 미만 → 다음 쌍으로 넘어가지 않음
- 판정 5초 미만 → 확인 안내 후 재선택
- 같은 이유 문장 반복 → 기존 `simText`(n-그램 자카드)로 직전 세 개와 0.8 이상 겹치면 안내
- 교사 화면에 중앙 판정 시간과 이상치를 표시(자동 삭제는 하지 않고 판단은 교사가 함)

---

## 7. 연구 자료로 무엇이 남는가

| 변수 | 계산 | 의미 |
|---|---|---|
| `bias1`, `bias2` | 예측 백분위 − 실제 백분위 | 자기평가 편향(+면 과대평가) |
| `calib` | \|bias1\| − \|bias2\| | **보정량. 이 설계의 주 결과 변수** |
| `selfShift` | 4축 평정의 ①→② 변화 | 자기 기준의 이동 방향 |
| `whyConc` | 이유 문장의 구체성(기존 `concreteness()` 재사용) | 판단 근거가 관찰 가능한 말로 쓰였는가 |
| `tagDist` | 결정적 축 태그 분포 | 학급이 무엇으로 작품을 가르는가 |
| `judgeFit` | 판정자 일관성 | 평가 기준의 내면화 정도 |
| `peerScore` | 브래들리–테리 θ | 동료 판단에 의한 작품의 위치 |
| `uptake` | `s8a.peerBack` 반영 여부 | 받은 문장이 실제 수정으로 이어졌는가 |

교차 검증으로 볼 것: `peerScore` ↔ 교사 루브릭 점수 ↔ 창의성 6축의 상관, 그리고 실제 하위권의 `bias1` 크기(과대평가 패턴).

「연구」 탭에 CSV 네 가지를 더합니다. `submissions.csv`(작품 단위) · `judgments.csv`(판정 단위, 익명 코드) · `selfeval.csv`(학생×단계) · `works_scores.csv`(집계 결과). 모두 동의한 학생만 포함합니다.

---

## 8. 수업 운영 (8차시 100분, 24명 기준)

| 시간 | 하는 일 | 화면 |
|---|---|---|
| 수업 전 | 최종 작품 제출(과제), 마감 후 교사가 **명단 확정** | 제출 |
| 0–5분 | 오늘 하는 일과 종합 질문 설명, 예시 한 쌍을 함께 판정 | 교사 시연 |
| 5–12분 | 자기평가 ① (등수 예측 포함) | 자기평가 ① |
| 12–30분 | 쌍대비교 12쌍 (쌍당 60~90초) | 쌍대비교 |
| 30–33분 | 교사 **집계 실행**, 반분 신뢰도 확인 | 교사 |
| 33–45분 | 자기평가 ②, ①과 나란히 보며 재평정, 받은 문장 읽기 | 자기평가 ② |
| 45–60분 | 결과 공개 후 토의: 예측과 실제가 가장 어긋난 지점, 축 태그 분포 | 결과 |
| 이후 | 8차시 기록 P·Q(비평과 성찰) 작성으로 이어감 | 기존 화면 |

---

## 9. 구현 계획

`src-app.jsx`는 여러 세션이 동시에 고치므로, 새 기능은 **새 파일**에 넣고 본 파일은 앵커 몇 줄만 건드립니다(`src-content.jsx`가 선례).

1. **체크포인트 커밋**: `git add -A && git commit`
2. **새 파일 `src-assess.jsx`**
   - 상수: `CRITERIA` · `PEER_QUESTION` · `DEFAULT_PEER_CFG`
   - 순수 함수: `pairsFor(roster, j, k)` · `bradleyTerry(items)` · `splitHalf(items)` · `judgeFit(items, θ)`
   - 학생: `<AssessTab me />`: 단계에 따라 제출·자기평가·비교·결과를 전환
   - 교사: `<AssessPanel ids roster />`: 단계 스위치·명단 확정·집계·순위표·CSV
   - 스타일: `<AssessStyle />`: 거대한 `CSS` 상수를 건드리지 않음
3. **`src-fb.js`**: `route()`에 `sub:` · `assess:` 두 줄, 컬렉션 전체를 읽는 `allAssess()` 하나
4. **`src-app.jsx` 앵커 편집, 네 곳뿐**
   - 상단 import 한 줄
   - `StudentApp`의 `sess-tabs` 렌더링 뒤에 조건부 「최종 평가」 탭
   - `TeacherApp`의 탭 배열에 `"상호평가"` 추가와 분기 한 줄
   - `<AssessStyle />` 마운트
5. **`firestore.rules`** 두 블록 추가 → `firebase deploy --only firestore:rules`
6. **번들·배포**: `npx esbuild src-app.jsx --bundle --jsx=automatic --loader:.jsx=jsx --minify --format=iife --outfile=public/app.js` → `firebase deploy --only hosting`
7. **README** 갱신(상호평가 절 추가)

작업 순서는 ① 데이터·규칙 → ② 제출 화면 → ③ 자기평가 화면(①②) → ④ 쌍대비교 화면 → ⑤ 집계·교사 화면 → ⑥ CSV이며, 단계마다 커밋합니다.

---

## 10. 확정이 필요한 결정

| 항목 | 기본값(제안) | 다른 선택지 |
|---|---|---|
| 회차 | 자기① – 비교 – 자기② 한 묶음 | 세 묶음 반복(두 차시 추가 필요) |
| 1인당 판정 수 k | 12 | 8(짧게) / 16(신뢰도 우선, 25분) |
| 결과 공개 범위 | 밴드(상·중·하) | 백분위 / 등수 / 비공개 |
| 작품 캡션 | 접힌 채 시작, 펼침 여부 기록 | 처음부터 공개 / 끝까지 가림 |
| 미제출자 | 판정에는 참여 | 판정도 제외 |
| 익명성 | 표시상 익명(작품 번호) | 서버 도입으로 완전 익명(후속 과제) |

이 여섯 가지만 정하면 곧바로 구현에 들어갈 수 있습니다.
