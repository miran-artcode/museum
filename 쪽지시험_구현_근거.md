# 미술사·미술이론 선택형 적응형 쪽지시험: 설계와 구현 근거

단원: 「실재한 적 없는 유물 이미지로 구성한 허구의 아카이브 전시」(고2 미술, 8차시) · 사이트: React + Firebase(서버 코드 없음) · 한 학급 약 25명 · 컴퓨터실 1회 응시 · 선택형만 · 총점 10점 · 제한 시간 40분

이 문서는 측정 설계(세 제안서와 세 심사 결과를 합친 최종 설계서)를 이 저장소의 구현(`src-quiz-core.mjs`, `src-quiz.jsx`, `src-quiz-teacher.jsx`, `src-fb.js`, `firestore.rules`)에 맞춰 옮긴 것이다. 측정 규칙(문항 수·라우팅·점수표·시간표·은행 청사진·사후 통계·잠금 규칙)은 설계서 그대로이고, 저장 구조와 보안 장치는 이 저장소의 관례(단일 앱, `fbStore` 봉투, 이메일 접두사 계정)에 맞게 바꿨다. 바꾼 점은 §0에 모아 적는다. 문헌은 §10에 있으며 확인되지 않은 것은 「확인 필요」로 남긴다.

---

## 0. 설계서와 구현이 다른 점

| 설계서 | 구현 | 까닭 |
|---|---|---|
| 은행을 AES-256-GCM으로 암호화하고 칠판의 암구호(PBKDF2)로 푼다 | 암호화 없음. 은행 공개부(`quizBank/v1`)는 정답 대신 `keyHash`만 담고, Firestore 규칙이 「교사가 시험을 연 뒤(stage가 closed가 아닐 때)」에만 학생 읽기를 허용한다. 정답·해설·출처는 교사만 읽는 `quizKeys/v1`에 따로 둔다 | 암호화해도 암구호가 칠판에 적히는 순간 누구나 풀 수 있고, 보기가 4개라 keyHash도 비밀이 아니다(§7.6). 학생 브라우저의 점수는 가채점이고 권위는 교사 재계산에 있으므로 암호화가 주는 것은 「소스를 대충 봐서는 안 보이게」뿐이다. PBKDF2 30만 회의 전날 점검과 암구호 입력 단계를 없애 운영을 줄였다 |
| `quizControl/{quizId}` 문서와 quizId | `meta/config`의 `quiz` 블록(`cfgAll.quiz`, 기본값 `DEFAULT_QUIZ`). 시험은 하나(`v1`)라 quizId가 없다 | 상호평가의 `peer` 블록과 같은 패턴. 교사 설정 저장은 `setDoc(merge)`로 필드만 병합한다 |
| `quizAttempts/{id}`와 하위 컬렉션 `blocks/{k}`(생성 전용)·`events/{autoId}`(생성 전용) | 단일 문서 `quiz/{학번}`. `v.blocks`·`v.served`는 「이미 있는 키는 바뀌지 않는다」 규칙(`quizBlocksKept`·`quizServedKept`)으로 불변을, `v.events`는 「길이가 줄지 않는다」 규칙(`quizEventsGrow`) + `arrayUnion`으로 추가 전용을 보장한다 | 하위 컬렉션은 규칙마다 부모 문서를 `get()`해야 하고(요청당 10회 한도), 교사 화면이 학생 25명 × 문서 수십 개를 구독해야 한다. 단일 문서는 규칙 평가 한 번에 `get(meta/config)` 1회로 끝나고 교사 화면은 컬렉션 하나만 본다. 이벤트는 300건 상한(`MAX_EVENTS`) |
| `quizResults/{id}` 별도 문서, `published`일 때 본인 읽기 | 응시 문서 안 `v.result`(교사만 씀). 학생은 자기 문서를 늘 읽을 수 있으므로 화면이 `stage == published`이고 `result.published`일 때만 결과를 그린다 | 문서 수를 줄인다. 학생이 공개 전에 `result`를 개발자 도구로 읽을 수는 있으나 그 값은 교사 재계산 결과이고 학생이 바꿀 수는 없다(§7.7의 정직한 한계에 포함) |
| 잠금 해제: 교사 화면 「해제」 또는 학생 PC에서 4자리 해제 번호 입력 | 교사 화면 「해제」만(`status: running`, `lockedAt: null`, 보전이면 `pausedTotalSec` 가산). 학생 화면은 자기 문서를 구독하다가 running으로 바뀌면 「계속하기」를 보인다 | 해제 번호는 학생 쓰기 규칙에 예외를 더 두어야 하고, 교사가 어차피 학생 자리로 간다 |
| 문항별 시간 `itemTimes`(화면에 머문 시간) | 블록 시간 `blockMs` + 선택 시각 `pickMs`(그 문항의 답을 마지막으로 고른 시각, 블록 시작 기준) | 5문항이 한 페이지에 모두 보이므로 「화면에 머문 시간」을 문항별로 잴 수 없다. 응답 시간 중앙값(§6.1)은 pickMs의 간격으로 근사한다 |
| seed = SHA-256(학번\|quizId\|seedSalt)의 앞 32비트, keyHash = SHA-256 | `deriveSeed = hash32(학번 + "\|" + seedSalt)`(FNV-1a 32비트), `keyHashOf = hash53(문항id\|보기id\|seedSalt).toString(36)`(cyrb53) | 순수 함수를 `node --test`로 돌리기 위해 WebCrypto(비동기·브라우저 전용)를 쓰지 않는다. 둘 다 비밀이 아니므로 암호학적 해시가 필요 없다 |
| 좌석표 `seatMap({학번: {row, col}})` | 좌석표 텍스트 `seatGrid`(줄 = 행, 칸은 공백·쉼표 구분, 빈자리 「-」) → `parseSeatGrid` | 교사가 텍스트 상자에 붙여 넣는 편이 빠르다. 반은 (행 + 열) mod 2로 같다 |
| 필드 이름 `heartbeatAt`·`sessionId`·`currentBlock`·`warnCount`·`clientCorrect` | `hb`·`sess`·`cur`·`warn`·`blocks[k].correct` | 짧은 이름. 봉투 밖 서버 시각 필드는 `startedAt`·`hb`·`lockedAt` 셋 |
| 이미지 사전 로드 실패 시 같은 칸의 다음 문항으로 자동 대체(`replaced`) | 두지 않음 | 이미지는 이미 학생이 본 차시 도판(`/img/lessons/`)이고 같은 호스팅에서 오므로 대체 규칙이 새 불일치 원인이 되는 것을 피했다. 로드 실패는 대체 텍스트로 풀린다(§5.6) |
| 「연결 끊김」 이벤트(`offline`) | 이벤트로 남기지 않고, `navigator.onLine`이 false이면 제출 버튼을 막고 「연결을 확인해 주세요」를 보인다 | 오프라인 큐가 있어 초안은 나중에 저장되고, 제출 시각은 서버 시각이어야 하므로 제출만 막으면 된다 |
| 교사 화면 알림음 | 없음(빨간 점·굵은 글자만) | 교사가 순시 중이라 소리가 닿지 않고, 학생 PC와 같은 실에서 소리는 방해가 된다 |
| 정답 키 오류 정정 → 「유리해진 학생은 F+1」(§4.6) | `recompute`가 `corrections`를 받아 정정 전후의 F를 비교해 처리한다 | 설계 그대로. 교사 화면 「문항 오류 정정」이 `quizKeys.corrections`에 항목을 더하고 「재계산」을 다시 돌린다 |

이 밖에 잠금 규칙(§7), 시간표(§3), 점수표(§4), 은행 크기(§5.3), 사후 통계(§6)는 설계서 값을 그대로 상수로 옮겼다(`src-quiz-core.mjs` 상단).

---

## 1. 한눈에 보는 규칙 (학생·학부모에게 그대로 보여 줄 5문장)

1. 5문항씩 4블록, 모두 **20문항**을 **40분** 안에 풉니다. 모든 학생이 가운데 급인 **3급**에서 시작합니다.
2. 한 블록에서 **4개 이상** 맞히면 다음 블록은 한 급 어려워지고, **3개**면 그대로, **2개 이하**면 한 급 쉬워집니다(가장 쉬운 1급과 가장 어려운 5급이 끝입니다).
3. **마지막 블록에서 어느 급에 있었는지가 등급(A~E)** 을 정합니다. 단, 마지막 블록에서 2개 이하를 맞히면 한 급 내려간 급이 최종급이고, 풀어 보지 않은 급으로 올려 주지는 않습니다.
4. **20문항 중 맞힌 수가 등급 안의 점수**를 정합니다. 빈칸은 오답과 같고 감점은 없으니 모르는 문항도 반드시 답하세요.
5. 시험 중에는 현재 급과 정답 여부를 보여 주지 않고, 시험이 끝나면 급 경로·블록별 정답 수·점수표의 내 행·화면 이탈 기록을 모두 공개합니다. 다른 창으로 나가면 기록되고, 세 번째에는 시험이 잠겨 선생님이 풀어 줍니다.

점수표(학생·학부모용, §4.2와 같음): 5급 → A(정답 18~20개 10점, 16~17개 9점, 14~15개 8점) · 4급 → B(14개 이상 7점, 13개 이하 6점) · 3급 → C 5점 · 2급 → D 4점 · 1급 → E(9개 이상 3점, 8개 이하 2점) · 응답 10문항 미만 → E 2점.

코드: `RULE_SENTENCES`, `SCORE_ROWS` (`src-quiz-core.mjs`).

---

## 2. 문항 수·급·블록·라우팅 규칙

### 2.1 구조 확정치

| 항목 | 확정 | 코드 |
|---|---|---|
| 학생 1인 문항 수 | **20문항** = 5문항 × 4블록 | `BLOCKS`, `BLOCK_SIZE` |
| 급 수 | 5급(1급 가장 쉬움 ~ 5급 가장 어려움) | `LEVELS` |
| 시작 급 | 3급, 전원 동일 | `START_LEVEL` |
| 블록 구성 | 한 블록은 한 급의 문항 5개. 영역 배분은 §5.2 | `AREA_MIX` |
| 라우팅 | 블록 정답 수 **4·5 → 한 급 상승**(5급이면 5급 유지), **3 → 유지**, **0·1·2 → 한 급 하강**(1급이면 1급 유지) | `routeNext`, `ROUTE_UP`, `ROUTE_DOWN` |
| 블록 안 이동 | 5문항이 한 페이지에 모두 보이고 자유롭게 답을 고칠 수 있다. 「블록 제출」을 누르면 잠기고 지난 블록으로 돌아갈 수 없다 | `blocks[k]` 불변 규칙 |
| 응답 강제 | 빈 문항이 있으면 제출 전에 한 번 확인 창을 띄운다(「빈 문항은 오답과 같습니다. 제출할까요?」). 강제로 막지는 않는다(시간이 끝나 가는 학생이 제출을 못 하는 일을 막기 위함) | |
| 보기 수 | 모든 급 4지선다(정답 1 + 오답 3) | `validateBank` |
| 도달 가능 급 | 블록 1 = 3급, 블록 2 = 2~4급, 블록 3·4 = 1~5급. 5급은 블록 3에서 처음 나타난다 | |
| 경로 수 | 급 경로 25가지(블록별 정답 수까지 세면 6⁴ = 1,296가지) | 테스트가 1,296경로를 전수 확인 |
| 급별 최대 방문 블록 수 | 1급 2, 2급 3, 3급 4, 4급 3, 5급 2 (열거 검산) | `CELL_MIN`의 근거 |
| 시험 중 표시 | 블록 번호(「블록 2 / 4」), 문항 번호, 답한 문항 수, 남은 시간, 블록별 권장 종료(§3). **현재 급·정오·상승/하강은 표시하지 않음** | `src-quiz.jsx` 상단 표시줄 |
| 시험 후 표시 | 급 경로, 블록별 정답 수, 최종급, 총 정답 수, 점수표의 자기 행, 문항별 정답·해설, 자기 이탈 기록 | `result`, `resultItems` |

### 2.2 급의 정의(교사 기준 다섯 표현과 인지 수준의 대응)

| 급 | 교사 기준 표현 | 인지 수준 | 문항이 요구하는 것 | 허용 자극 | 설계 목표 정답률 |
|---|---|---|---|---|---|
| 1급 | 가장 낮은 단계 | 용어 인지 | 용어·이름·제목의 뜻과 범주를 알아본다 | 용어 하나, 한 줄 정의 | .85 |
| 2급 | 낮은 단계 | 사실 연결 | 작가↔작품↔연도↔개념을 짝짓는다 | 작가·작품·연도, 강의 노트 도판 | .75 |
| 3급 | 가운데 단계 | 개념 이해 | 개념의 「왜」, 개념 사이의 차이를 가린다 | 개념 설명문(노트 문장 재구성) | .60 |
| 4급 | 높은 단계 | 익숙한 사례 적용 | 강의 노트의 사례를 새 각도에서 개념에 연결한다 | 노트의 사례(60편·발문 151개·도판 42장)를 1~3문장으로 다시 서술 | .50 |
| 5급 | 가장 어려운 단계 | 처음 보는 사례 적용·분석 | 노트에 없는 새 사례를 문항 안에서 읽고 환유·물질문화·사진 지표성·파라픽션·제도비평의 관점으로 판단한다 | 문항 안에 완결된 새 사례(텍스트, 140자 이내) | .40 |

코드: `LEVEL_NAMES`, `LEVEL_WORDS`, `LEVEL_TARGET_P`.

### 2.3 라우팅 임계값의 근거(4지선다, 이항분포)

| 학생의 참 정답률 p | 상승(4개 이상) | 유지(정확히 3) | 하강(2개 이하) |
|---|---|---|---|
| .25(순수 추측) | **.016** | .088 | .897 |
| .30 | .031 | .132 | .837 |
| .50 | .188 | .312 | .500 |
| .60 | .337 | .346 | .317 |
| .70 | .528 | .309 | .163 |
| .80 | .737 | .205 | .058 |
| .90 | .919 | .073 | .009 |

- 순수 추측이 블록 1~3의 세 번 이동에서 한 번이라도 오를 확률은 1 − (1 − .0156)³ = 4.6%, 두 번 연속 오를 확률은 .0156² = 0.02%.
- 3지선다이면 순수 추측 상승이 .045(약 3배), 4문항 묶음 3개 이상이면 .051이라 4지선다 5문항을 택했다.
- 유지 구간이 「정확히 3」 하나뿐이라 p=.6인 학생은 한 블록에서 세 방향으로 거의 고르게 갈린다. 이것이 20문항 설계의 본질적 한계이며 §9에 적는다.

### 2.4 미응답 처리

- 빈 문항은 **오답과 같이** 센다(감점 없음). 라우팅에도 오답으로 들어간다.
- 타이머가 끝나면 현재 블록이 그대로 자동 제출된다(선택한 것까지만, `auto: true`). 시작하지 못한 블록은 0/5로 처리해 라우팅 규칙을 그대로 적용한다(남은 블록 수만큼 하강).
- 응답 문항 수 n(답을 고르고 제출된 문항 수)이 **10 미만**이면 「끝까지 응답하지 못함」으로 E 2점(`MIN_ANSWERED`). n ≥ 10이면 위 규칙대로 정상 산출한다.

---

## 3. 40분 시간표

### 3.1 급별 권장 시간과 경로별 소요

| 급 | 문항당 권장 | 블록(5문항) |
|---|---|---|
| 1급 | 40초 | 3분 20초(200초) |
| 2급 | 50초 | 4분 10초(250초) |
| 3급 | 70초 | 5분 50초(350초) |
| 4급 | 90초 | 7분 30초(450초) |
| 5급 | 120초 | 10분 00초(600초) |

25가지 급 경로를 전부 계산한 결과:

| 경로 | 소요 | 비고 |
|---|---|---|
| 3→4→5→5(최장) | 350+450+600+600 = 2,000초 = **33분 20초** | 40분에서 6분 40초(17%) 여유 |
| 3→3→3→3 | 1,400초 = 23분 20초 | 가장 흔한 C 경로 |
| 3→2→1→1(최단) | 1,000초 = 16분 40초 | |

5급 문항의 사례 서술을 140자 이내로 제한하는 것(§5.6)이 「5급 120초」의 전제다. 5급 문항이 150초씩 걸려도 최장 경로는 38분 20초로 40분 안이다. 코드: `LEVEL_SEC`.

### 3.2 학생 화면의 권장 종료 시각(급을 드러내지 않는 방식)

급을 숨기므로 급별 권장 시간을 그대로 보이면 급이 새어 나간다. 대신 **블록 번호 기준 누적 권장 종료 시각**을 보인다. 최장 경로(5:50 / 13:20 / 23:20 / 33:20)에 여유를 더한 값이다.

| 블록 | 권장 종료(시작 후) |
|---|---|
| 블록 1 | 7분 |
| 블록 2 | 15분 |
| 블록 3 | 25분 |
| 블록 4 | 35분 |

남은 시간 10분·5분·1분에 화면 위 표시줄로 조용히 알린다(소리 없음, `aria-live="polite"`). 문항별·블록별 제한 시간은 두지 않는다. 코드: `BLOCK_END_MIN`.

### 3.3 수업 50분 운영표(개인 타이머 40분은 학생이 「시험 시작」을 누른 서버 시각부터)

| 구간 | 시각 | 내용 |
|---|---|---|
| 안내 | 0:00~3:00 | 교사가 §1의 5문장, 잠금 규칙, 이의 절차를 구두로 말한다. 학생은 로그인한다(암구호 없음, §0) |
| 연습 | 3:00~6:00 | 연습 2문항(은행의 `practice`, 1급·3급 각 1, 채점 즉시 표시·시간 없음) → 「화면 이탈 경고 체험」 버튼으로 경고 화면을 한 번 본다 |
| 본시험 | 6:00~46:00 | 교사가 「열기」를 누르면 학생의 「시험 시작」 버튼이 활성화됩니다. 시작 마감은 연 뒤 **15분**(`startDeadlineSec`, 그 뒤 새로 시작 불가 → 결시 처리, 별도 응시) |
| 마무리 | 46:00~50:00 | 늦게 시작한 학생 대기, 교사 화면에서 전원 「완료」 확인, 「시작 마감」 단계 |

수업이 40분 단위인 학교는 안내·연습을 전 차시 끝에 하고 본시험만 40분에 넣는다. 늦게 시작한 학생도 자기 40분을 온전히 받는다(다음 수업 담당 교사와 사전 합의).

### 3.4 시간 배수와 잠금·정지

- 교사는 시험 설정에서 학생별 **시간 배수 1 / 1.5 / 2**(학습지원·특수교육 대상)를 지정한다(`cfg.timeMult[학번]`). 그 학생의 화면은 다른 학생과 똑같이 보인다.
- 잠금(§7.3) 중에는 타이머가 멈추고, 교사가 해제할 때 「시간 보전」(기본 켜짐)을 두면 멈춘 시간이 `v.pausedTotalSec`에 더해진다. 학생이 일부러 잠금을 얻어 쉬는 것을 막으려면 교사가 보전을 끈다.
- 전체 일시정지(정전·서버 장애): 교사 화면 「전체 일시정지」 → 모든 학생 화면이 멈춤 → 재개 시 멈춘 초가 `cfg.pausedAccumSec`에 더해진다.
- 남은 초 = `durationSec × 배수 + pausedTotalSec + extraSec + pausedAccumSec − (서버 현재 시각 − startedAt)` (`remainingSec`). 규칙의 `quizWithinTime`도 같은 식에 60초 유예를 더해 계산한다(§8.3).

---

## 4. 점수 산출식과 A~E 대응표

### 4.1 변수

| 변수 | 정의 |
|---|---|
| L_k, k_k | 블록 k(1~4)의 급과 정답 수 |
| **F(최종급)** | L_4에서, k_4 ≤ 2이면 max(1, L_4 − 1), 아니면 L_4. 위로는 올리지 않는다 |
| **c(총 정답 수)** | 20문항 중 정답 수. 빈 문항·미시작 블록은 0 |
| n(응답 문항 수) | 답을 고르고 제출된 문항 수(0~20) |

### 4.2 산출표(확정)

우선순위: ① n < 10이면 2점. ② 그 외는 F로 등급, c로 등급 안의 점수.

| F | c | 점수 | 등급 | 이 행에 이르는 c의 구조적 범위 |
|---|---|---|---|---|
| 5 | 18~20 | 10 | A | 5급 최종급의 c 범위는 14~20 |
| 5 | 16~17 | 9 | A | |
| 5 | 14~15 | 8 | A | c=14가 구조적 최솟값(§4.3) |
| 4 | 14~17 | 7 | B | 4급 최종급의 c 범위는 11~17 |
| 4 | 11~13 | 6 | B | |
| 3 | 제한 없음 | 5 | C | 3급 최종급의 c 범위는 8~15 |
| 2 | 제한 없음 | 4 | D | 2급 최종급의 c 범위는 7~14 |
| 1 | 9 이상 | 3 | E | 1급 최종급의 c 범위는 0~12 |
| 1 | 8 이하 | 2 | E | 순수 추측과 구별되지 않는 정답 수 |
| F 무관 | n < 10 | 2 | E | 끝까지 응답하지 못함 |

E의 2·3점 경계 근거: 20문항 순수 추측(p=.25)에서 P(c ≥ 9) = .041, P(c ≥ 8) = .102. c ≥ 9는 5% 수준에서 추측과 구별되므로 「1급에 머물렀으나 용어는 안다」로 3점, c ≤ 8은 추측 수준으로 2점이다. 코드: `SCORE_ROWS`, `scoreOf`, `scoreAttempt`.

### 4.3 「5급 최종급이면 c ≥ 14이고 5급 누적 정답률 ≥ 60%」의 증명

F = 5이려면 L_4 = 5이고 k_4 ≥ 3이어야 한다. L_4 = 5인 경로는 셋뿐이다.

| 경로 | 조건 | c의 최솟값 |
|---|---|---|
| 3→4→5→5 | k_1 ≥ 4, k_2 ≥ 4, k_3 ≥ 3(5급 유지), k_4 ≥ 3 | 4+4+3+3 = 14 |
| 3→4→4→5 | k_1 ≥ 4, k_2 = 3, k_3 ≥ 4, k_4 ≥ 3 | 4+3+4+3 = 14 |
| 3→3→4→5 | k_1 = 3, k_2 ≥ 4, k_3 ≥ 4, k_4 ≥ 3 | 3+4+4+3 = 14 |

첫 경로에서 5급 두 블록은 각각 3개 이상이므로 5급 누적 정답률은 6/10 ≥ .60, 나머지 두 경로는 5급 한 블록 3/5 ≥ .60이다. 따라서 「확보 급 S ≥ .60」 조건은 별도 규칙 없이 대칭 라우팅에 내장된다. A 8점의 하한 70%는 「정답률이 매우 높다」에 가깝다. 테스트(`src-quiz-core.test.mjs`)가 1,296경로 전수로 이 성질을 확인한다.

### 4.4 교사 기준 서술문과 산출 변수의 대응

| 등급 | 교사 기준 서술 | 대응 변수 |
|---|---|---|
| A | 가장 어려운 단계의 문항까지 도달 | L_4 = 5(5급 블록을 최소 1회, 최대 2회 풂) |
| A | 정답을 유지하며 | 5급 모든 블록에서 3개 이상(k_4 ≥ 3이고, 두 번 방문했으면 k_3 ≥ 3) |
| A | 응답한 문항의 정답률이 매우 높다 | c ≥ 18(90%) 10점, 16~17(80~85%) 9점, 14~15(70~75%) 8점 |
| A | 흐름·환유와 물질문화·사진의 증거성을 처음 보는 사례에 적용 | 5급 블록의 영역 배분 ①1·②2·③2(§5.2) |
| B | 높은 단계의 문항에 도달, 정답률 높은 편 | F = 4. c ≥ 14(70%) 7점, 11~13(55~65%) 6점 |
| C | 가운데 단계에서 머무름, 절반을 조금 넘는 정답 | F = 3. 3급에 네 블록 머물며 매번 3/5이면 c = 12(60%). 범위 8~15 |
| D | 낮은 단계에서 머무름, 정답률 낮은 편 | F = 2. 범위 7~14 |
| E | 가장 낮은 단계에 머무름 | F = 1: c ≥ 9 → 3점, c ≤ 8 → 2점 |
| E | 배정된 문항을 끝까지 응답하지 못함 | n < 10 → 2점 |

### 4.5 경계 사례(교사가 학생에게 보여 줄 표)

| 사례 | 블록 1(3급) | 블록 2 | 블록 3 | 블록 4 | F | c | n | 점수 | 설명 |
|---|---|---|---|---|---|---|---|---|---|
| 가 | 4/5 ↑ | 4급 4/5 ↑ | 5급 3/5 = | 5급 3/5 = | 5 | 14 | 20 | **A 8** | A의 최솟값. 5급 6/10 유지 |
| 나 | 5/5 ↑ | 4급 5/5 ↑ | 5급 4/5 = | 5급 2/5 ↓ | 4 | 16 | 20 | **B 7** | 5급까지 도달했으나 마지막에 유지 못함 |
| 다 | 4/5 ↑ | 4급 4/5 ↑ | 5급 2/5 ↓ | 4급 4/5 (다음 블록 없음) | 4 | 14 | 20 | **B 7** | 위로 올리지 않음 |
| 라 | 5/5 ↑ | 4급 2/5 ↓ | 3급 5/5 ↑ | 4급 2/5 ↓ | 3 | 14 | 20 | **C 5** | 4급 누적 4/10. 「경로 민감」 표시 대상 |
| 마 | 3/5 = | 3급 3/5 = | 3급 3/5 = | 3급 3/5 = | 3 | 12 | 20 | **C 5** | 전형적 C(60%) |
| 바 | 2/5 ↓ | 2급 4/5 ↑ | 3급 4/5 ↑ | 4급 3/5 = | 4 | 13 | 20 | **B 6** | 하강 뒤 회복 |
| 사 | 2/5 ↓ | 2급 2/5 ↓ | 1급 3/5 = | 1급 3/5 = | 1 | 10 | 20 | **E 3** | 1급에 머물렀으나 추측 수준 아님 |
| 아 | 1/5 ↓ | 2급 1/5 ↓ | 1급 2/5 ↓ | 1급 2/5 | 1 | 6 | 20 | **E 2** | 추측과 구별 안 됨(c ≤ 8) |
| 자 | 5/5 ↑ | 4급 5/5 ↑ | 5급 4/5 = | 5급 시간 종료, 2문항 응답 1/5 | 4 | 15 | 17 | **B 7** | 절벽 없음: 빈 3문항이 오답으로 들어가 한 급 내려감 |
| 차 | 3/5 = | 3급 2/5 ↓ | 2급 시간 종료, 3문항 응답 2/5 | 미시작 0/5(1급) | 1 | 7 | 13 | **E 2** | n ≥ 10이지만 미시작 블록의 0/5가 라우팅에 들어감 |
| 카 | 3/5 = | 3급 4/5 ↑ | 4급 시간 종료, 1문항 응답 1/5 | 미시작 0/5(3급) | 2 | 8 | 11 | **D 4** | 3급→4급 도달 뒤 시간 부족 |

「나」와 「가」의 대비가 「도달」과 「유지」의 차이를, 「라」가 총 정답 수가 같아도 마지막 급이 다르면 점수가 다름을 설명한다. 이 11개 사례는 그대로 회귀 테스트에 들어 있다.

### 4.6 문항 오류·재채점·이의 규칙(시험 전에 공지)

1. 문항 오류(정답 둘·정답 없음·오타로 뜻이 바뀜)가 확인되면 그 문항은 **전원 정답 처리**한다(`quizKeys.corrections`).
2. 재채점은 실제로 받은 문항과 실제 경로를 바꾸지 않고 c와 F만 다시 계산한다. 단, 정답 처리로 어느 블록에서든 이동이 원래보다 유리해지는 학생(하강이 유지로, 유지가 상승으로 바뀌는 경우)은 재계산한 F에 **한 급을 더한다(최대 5급, 블록 수와 무관하게 +1)**. `recompute`가 정정 전후의 F를 비교해 처리한다.
3. 기술 장애(정전·네트워크·PC 고장) 때문에 끝내지 못한 경우는 2점을 확정하지 않고 §7.5의 재개 또는 별도 응시로 구제한다.
4. 이의 제기는 「응시 기록표」(§6.3) 한 장으로 답한다. 산출식 자체를 사후에 바꾸지 않는다.
5. 안내 자료에 「이 검사의 등급 판정은 ±1등급의 오차를 가질 수 있다」를 넣는다(§4.7).

### 4.7 규칙이 프로파일을 제대로 가르는가(몬테카를로 20,000회)

급별 참 정답률을 가정한 가상 학생을 위 규칙으로 돌렸다(설계 단계의 검산 스크립트 `final_sim.py`).

| 프로파일(1~5급 정답률) | A | B | C | D | E | 최빈 점수 |
|---|---|---|---|---|---|---|
| A형 .97/.95/.92/.88/.80 | **.86** | .14 | .00 | | | 10(.41), 9(.39) |
| A−형 .95/.92/.88/.80/.70 | **.62** | .34 | .04 | | | 9(.35), 7(.30) |
| B형 .95/.90/.82/.68/.50 | .19 | **.58** | .21 | .02 | | 7(.40) |
| C형 .90/.80/.62/.45/.35 | .01 | .15 | **.50** | .31 | .02 | 5(.50) |
| D형 .85/.65/.45/.35/.28 | | .02 | .19 | **.56** | .23 | 4(.56) |
| E형 .65/.45/.33/.28/.25 | | | .03 | .24 | **.74** | 3(.40), 2(.33) |
| 순수 추측 .25 | | | .00 | .01 | **.985** | 2(.95) |

4문항 묶음 원안의 재현 시뮬(B→B .40, C→C .34, D→D .36, E→E .44)보다 모든 밴드에서 낫고, 25문항 5모듈안(B .61, C .53, D .59, E .79)에 가깝다. 순수 추측이 D 이상을 받을 확률은 1.5%다. 인접 밴드 번짐 15~35%는 20문항의 한계로 §9에 적는다.

---

## 5. 문항 은행 청사진

### 5.1 영역

| 영역 | 이름 | 내용(차시) | 성취기준 |
|---|---|---|---|
| ① | 미술사의 흐름 | 뒤샹 「샘」과 그 이전 네 해(개입의 정도·다섯 가지 조작), 르윗 선언·월 드로잉, 단토 미술계·디키 제도론(브릴로 상자), 코수스 「하나 그리고 세 개의 의자」, 극사실주의·리히터·사진처럼 보이는 여섯 단서, 다섯 후보, 캡션이 만드는 두 사물, 미술 개념의 역사성, 맥락의 여섯 장치, 브로타에스·하케·프레이저 제도비평, 아카이브 충동(포스터), 파라픽션(램버트-비티·폰트쿠베르타·아틀라스 그룹), 텍스트 미술(코수스·홀저·위너)(1·2·6차시) | [12미03-01] [12미감01-01] |
| ② | 사물·흔적·환유 | 환유(살세도·보디치코·임흥순·포렌식 아키텍처: 사람이 없는 자리에 사물이 남는다), 세미오포어(포미안), 호더 「유물은 텍스트다」, 사용흔·행위연쇄(물질문화), 쓰레기 고고학(래스제), 흔적의 인과 사슬, 명명 규칙(재질·특징·기물 종류), 세 시점 드로잉, 수리의 윤리(3·4·6차시) | [12미감01-01] [12미03-01] |
| ③ | 이미지와 증거 | 조작은 AI와 함께 오지 않았다(사진 조작사), 지표성(퍼스·바르트·세 겹의 신뢰), 기계 이미지 100년 논쟁, 프롬프트는 명세서, 다섯 번의 가설 검증·계획된 우연, 이중 점검(있는가·맞는가·남는가), 저자성, 핍진성의 계보와 무너지는 순간, 캡션의 힘(사진은 스스로 말하지 못한다), 사진과 텍스트의 병치, 작품 캡션 일곱 칸, 허구 고지, 사진·합성·생성 세 패널, 이미지 윤리(1·2·5·6·8차시) | [12미01-02] |
| ④ | 관찰·사회·전시·비평 | 관찰 절차 다섯(표류·정점 관찰·동선 지도·사물 목록·소리 산책), 랑시에르 감각적인 것의 분배, 손택·대변의 문제, 알프레도 자 르완다 프로젝트, 사회를 드러내는 여덟 갈래와 비숍/케스터/부리요, 왜 보이지 않는가 다섯 갈래, 번역 사다리, 화이트 큐브(오도허티), 관람자에게 건네는 세 글, 펠드먼 네 단계, 허구를 밝히는 사용이 훈련한 것(3·7·8차시) | [12미01-02] [12미창01-01] |

[12미창01-01](진로·관심 분야와 연결된 주제 탐색)은 선택형으로 직접 재기 어렵다. 영역 ④의 「이 주제에 맞는 관찰 절차·자료 수집 방법은」형 문항으로 간접 측정하고, 이 성취기준은 주로 작품·상호평가에서 평가된다고 성적 산출 계획에 적는다. 코드: `AREAS`.

### 5.2 블록 안 영역 배분

| 급 | ① | ② | ③ | ④ | 계 |
|---|---|---|---|---|---|
| 1~4급 | 2 | 1 | 1 | 1 | 5 |
| 5급 | 1 | 2 | 2 | 0 | 5 |

1~4급에서 ①을 두 배로 두는 까닭은 B·C 서술이 「주요 개념과 대표 작가의 연결」을 먼저 들고 1·2차시 읽기 자료가 가장 많기 때문이다. 5급에서 ④를 빼고 ②③을 두 배로 두는 까닭은 A 서술이 「환유와 물질문화의 관점, 사진이 증거로 읽히는 까닭을 처음 보는 사례에 적용」을 A의 표지로 들기 때문이며, ④는 4급까지에서 충분히 측정된다. 코드: `AREA_MIX`.

### 5.3 A·B 반(半)과 은행 크기: 최악 경로에서 반복 없음 보장

- 컴퓨터실 좌석을 바둑판 두 색으로 나눈다: **반 = (행 + 열) mod 2 → 0이면 A, 1이면 B**. 교사가 시험 설정의 좌석표(`seatGrid`)에 학번 격자를 입력한다. 좌석표가 없으면 학번 마지막 숫자의 홀짝으로 대체하되 교사 화면에 「좌석표 없음: 인접 회피 보장 안 됨」을 표시한다(`halfOf`의 `known: false`).
- 각 (급, 영역) 칸을 A반·B반으로 반씩 나눈다. A색 학생은 A반 칸에서만 뽑는다. 따라서 **앞·뒤·좌·우 이웃과는 어떤 문항도 겹치지 않는다.** 대각선 이웃(같은 색)과는 겹칠 수 있으나 문항 순서·보기 순서가 다르고 진행 속도가 다르다.
- 한 반의 칸 크기 ≥ (그 급 최대 방문 블록 수) × (블록당 그 영역 문항 수)이면 어떤 경로에서도 반복이 없다.

| 급 | 최대 방문 | 반당 최소(①/②/③/④) | 반당 확정(①/②/③/④) | 급 합계(A+B) |
|---|---|---|---|---|
| 1급 | 2 | 4/2/2/2 = 10 | 4/2/2/2 = 10 | 20 |
| 2급 | 3 | 6/3/3/3 = 15 | **7**/3/3/3 = 16 | 32 |
| 3급 | 4 | 8/4/4/4 = 20 | **9**/4/4/4 = 21 | 42 |
| 4급 | 3 | 6/3/3/3 = 15 | **7**/3/3/3 = 16 | 32 |
| 5급 | 2 | 2/4/4/0 = 10 | 2/**5**/**5**/0 = 12 | 24 |
| 계 | | 70 × 2 = 140 | 75 × 2 = **150** | **150** |

- 최솟값 140에 예비 10을 더해 **실전 은행 150문항**(제약 100~160 안). 예비는 가장 많이 방문하는 3급 ①, 이미지 문항이 들어가는 2·4급 ①, 검토 탈락률이 가장 높은 5급 ②③에 두었다. 예비 문항은 따로 두지 않고 칸에 넣어 추출 다양성을 높인다. 코드: `CELL_MIN`, `validateBank`가 모자란 칸을 오류로 잡는다.
- 초안은 **170문항**(+13%)을 만들고 검증에서 탈락시켜 150을 남긴다. 탈락이 특정 칸에 몰리면 그 칸만 다시 초안한다. 연습 2문항은 은행 파일의 `practice`에 따로 둔다.
- 교사 검토 부담: 문항당 3분 × 150 = 7.5시간. 쌍둥이(§5.7)를 나란히 보면 75쌍이라 실제로는 더 짧다. 2주에 걸쳐 급별로 나눠 검토한다.

### 5.4 학생별 추출 규칙(구현 명세)

1. `seed = deriveSeed(학번, seedSalt)` = FNV-1a 32비트 해시(학번 + "|" + seedSalt) → `mulberry32` PRNG. `seedSalt`는 은행 문서 안에 있어 교사가 시험을 열기 전에는 읽을 수 없다. 계산된 seed는 응시 문서 `v.seed`에 기록한다.
2. 자기 반의 각 (급, 영역) 칸을 `cellOrder(seed, 급, 영역, 칸)`으로 한 번씩 섞는다(칸마다 seed와 칸 이름을 섞어 파생한 난수). 문항은 id 정렬 뒤에 섞으므로 은행 파일의 순서와 무관하다.
3. 블록 k의 급 L이 정해지면 `drawBlock`이 `AREA_MIX[L]` 순서(① → ④)로 각 칸을 훑으며 아직 나오지 않은(`used`) 문항과 비활성이 아닌 문항을 앞에서부터 고른다. 급이 바뀌었다 돌아와도 `used`(지금까지 `served`의 모든 문항)가 남아 있어 이미 본 문항은 다시 나오지 않는다.
4. 이미지 문항 규칙: 블록에 이미 이미지 문항이 하나 있으면 다음 이미지 문항은 **소비하지 않고 건너뛴다**(뒤로 미룸). 「이미지 문항 제외」 학생(`cfg.noImage[학번]`)은 이미지 문항을 항상 건너뛴다. 칸이 바닥나면 다른 반의 같은 칸(같은 seed의 `cellOrder`)에서 꺼내고 `crossHalf: true`를 기록한다(정상 경로에서는 일어나지 않고, 이미지 제외·교사의 문항 비활성화 때만 일어난다).
5. 블록 안 5문항의 순서는 `mulberry32(seed ^ hash32("blk|" + k))`로, 각 문항의 보기 순서는 `optionOrder(seed, 문항)` = `mulberry32(seed ^ hash32("opt|" + 문항id))`로 섞는다. 「모두 옳다」류의 위치 고정 보기는 쓰지 않는다.
6. 뽑힌 문항 ID 5개와 급을 블록을 열 때 `served[k]`에 기록한다(이후 불변, 규칙 `quizServedKept`). 채점·재계산은 이 기록을 기준으로 하며, 교사 화면은 `replayServed`로 같은 추출을 재현해 기록과 집합으로 대조한다(§6.2 「불일치」).

### 5.5 보기 수와 공통 작성 규칙

- **4지선다**로 통일해 급이 보기 수로 드러나지 않게 한다. 근거는 §2.3(3지선다는 추측 상승이 3배, 5지선다는 다섯째 오답이 「버리는 보기」가 되기 쉬움. Rodriguez 2005의 3지선다 최적론은 문항 합산 검사에 해당하고, 이 검사는 블록 정답 수로 급을 결정하므로 추측률이 라우팅 오판에 직접 들어간다).
- 공통 규칙(Haladyna, Downing & Rodriguez 2002를 급별로 좁힘): 문두에 문제를 완결해 쓴다 · 「모두 옳다/없다」 금지 · 보기 길이를 비슷하게(가장 긴 보기가 정답인 비율은 은행 전체 25% ± 5%) · 문법·어미 단서 제거 · 한 문항에 하나의 판단만 · 같은 반 안의 다른 문항이 정답을 알려 주지 않게(교차 점검) · 부정 문두(「옳지 않은 것」)는 3급 이상에서 블록당 최대 1문항, `neg: true`로 표시하고 굵은 밑줄로 보인다.
- 오답 3개는 강의 노트의 실제 오개념에서 가져온다(예: 「샘」이 거부된 이유, 지표성과 도상성의 혼동, 파라픽션과 단순 허구의 차이, 제도론과 취향의 혼동).
- 모든 문항은 근거 필드(`src: { lesson, h, quote }`: 차시·소제목·인용)를 갖는다. 검토 시 「그 자료만 읽은 학생이 답할 수 있는가」를 확인하고, 이의 답변에 이 근거를 보인다.

### 5.6 급별 문항 형식 규칙

| 급 | 문두 길이 | 허용 자극 | 금지 | 이미지 |
|---|---|---|---|---|
| 1급 | 60자 이내 | 용어·이름·제목 하나와 한 줄 정의 | 사례 서술, 부정 문두, 개념 비교 | 불가 |
| 2급 | 80자 이내 | 작가·작품·연도·매체·개념의 짝. 노트 도판으로 「이 작품의 작가」식 식별 가능 | 개념 간 비교, 부정 문두 | 가능(①③ 칸) |
| 3급 | 120자 이내 | 개념 설명문(노트 문장 재구성), 「~인 까닭은」「~와 달리」형 | 노트 밖 사례 | 가능(①③ 칸) |
| 4급 | 160자 이내 | 노트의 사례·도판을 1~3문장으로 다시 서술하고 새 물음으로 | 노트 밖 사례, 노트 문장 그대로 인용 | 가능(①③ 칸) |
| 5급 | 사례 서술 **140자 이내**, 문두 전체 220자 이내 | 노트에 없는 새 사례(가상 사례 원칙, 「가상 사례」 표시). 실존 작품을 쓰면 필요한 사실을 문항 안에 모두 적음. 물음은 관점 적용형 | 문항 밖 배경지식이 필요한 물음, 두 문항이 같은 사례를 공유(국소 의존), 이미지 | 불가 |

이미지 문항: 노트 도판 42장만 쓴다(학생이 이미 본 이미지, 저작권·로딩·표시 일관성). 2·3·4급의 ①③ 칸에만, 반당 칸마다 최대 1개(은행 전체 최대 12개, 8%), 블록당 최대 1개. `/img/lessons/`의 도판을 그대로 쓴다. 모든 이미지 문항은 텍스트만으로도 풀려야 하고(대체 텍스트는 노트 캡션 그대로: 작가·제목·연도·재료), 검증 단계에서 「대체 텍스트만 읽고 풀리는가」와 「이미지 없이 텍스트만으로 풀리는가」를 둘 다 확인한다. 코드: `STEM_MAX`, `CASE_MAX_5`, `validateBank`.

### 5.7 문항 제작·검증 절차

1. 교사가 급·영역별 **명세 75개**(급·영역·묻는 개념·자극 종류·오답이 노릴 오개념)를 확정한다. 명세 하나에서 **쌍둥이 2문항**(A반·B반, 같은 `spec`)이 나온다. 작성 단계에서 두 반의 난이도를 맞추는 유일한 수단이다.
2. AI가 강의 노트에서 쌍둥이 문항을 초안하고 근거 필드를 붙인다(초안 170).
3. 자동 점검(`validateBank`): 정답 유일·보기 4개·문두 길이 한도·부정 문두 표시·급별 금지 자극·칸 크기·쌍둥이가 A·B 각 1개·이미지 경로. 보기 길이 편차와 같은 반 안 정답 누설은 사람이 본다.
4. 독립 AI 풀이 점검(강의 노트 없이 문항만 주고 풀게 함): 1·2급 정답률이 100%에 가깝지 않으면 급을 올리고(용어 인지가 아니라 추론을 요구), 5급을 사례 서술 없이도 맞히면 사례를 다시 쓴다(배경지식으로 풀림). 4급을 노트 사례 요약 없이 맞히면 3급으로 내린다.
5. 교사 최종 확인(쌍으로 나란히). **5급은 교사 전수 검토 + 동료 교사 맹검**(문항만 보고 「정답이 하나뿐인가」). 확인 도장이 없는 문항은 은행에 들어가지 않는다.
6. 가능하면 동료 교사 또는 전년도 학생 3명이 3급 블록 하나를 풀어 70초/문항이 맞는지 확인한다.

은행 파일(`quiz-bank/bank.json`)은 번들에 넣지 않고 교사 화면 「문항 은행」 카드에서 올린다. `splitBank`가 공개부(`quizBank/v1`, 정답 대신 `keyHash`)와 정답부(`quizKeys/v1`)로 나눈다.

### 5.8 급별 예시 문항(강의 노트 내용, 각 급 1개)

**1급 · 영역 ① (A반)** 「레디메이드(ready-made)」의 뜻으로 가장 알맞은 것은?
① 공장에서 만든 기성품을 작가가 골라 작품으로 제시한 것 ② 사진을 그대로 옮겨 그린 극사실 회화 ③ 관람자가 참여해야 완성되는 작품 ④ 지시문에 따라 벽에 직접 그린 드로잉
정답 ①. 근거: 1차시 「샘」 자료. 오답은 극사실주의(②)·관계 미술(③)·르윗 월 드로잉(④)과의 범주 혼동.

**2급 · 영역 ① (A반)** 1968년 자기 집에 「현대미술관 독수리부」를 열어 미술관의 분류·전시 장치를 작품으로 삼은 작가는?
① 브로타에스 ② 하케 ③ 프레이저 ④ 코수스
정답 ①. 근거: 2차시 브로타에스 자료. 오답은 같은 제도비평 계보의 작가(②③)와 개념미술(④).

**3급 · 영역 ③ (A반)** 「사진은 대상에서 온 빛이 감광면에 직접 남긴 흔적이다.」 이 성질(지표성)로 사진이 그림과 달리 「증거」로 읽히는 까닭을 설명할 때 핵심은?
① 대상이 카메라 앞에 있었기에 흔적이 남았다는 인과 관계 ② 사진이 대상과 매우 닮았다는 점 ③ 사진가가 진실을 말하기로 약속했다는 점 ④ 인화지가 그림보다 오래 보존된다는 점
정답 ①. 근거: 5차시 지표성(퍼스) 자료. 오답 ②는 도상성과의 혼동, ③은 상징(약속)과의 혼동, ④는 무관한 물성.

**4급 · 영역 ② (A반)** 살세도는 실종된 사람들이 쓰던 가구와 신발을 작품의 재료로 쓴다(3차시). 강의 노트가 이 방식을 「환유」라고 부르는 까닭은?
① 사물이 그 사람 곁에 있었기에, 사람이 없는 자리에서 사람을 대신 가리키기 때문 ② 사물이 사람의 모습과 닮았기 때문 ③ 사물에 사람의 이름이 적혀 있기 때문 ④ 관람자가 사물을 만져 작품을 완성하기 때문
정답 ①. 근거: 3차시 환유 자료(「사람이 없는 자리에 사물이 남는다」). 오답 ②는 은유·도상과의 혼동, ③은 명명과의 혼동, ④는 관계 미술과의 혼동.

**5급 · 영역 ③ (A반, 가상 사례 93자)** 한 지역 신문이 홍수 피해 기사에 진흙에 반쯤 묻힌 가족사진 한 장을 실었다. 뒤에 그 사진은 기자가 다른 마을에서 주워 온 사진을 현장에 놓고 찍은 것으로 밝혀졌다. 이 사진이 처음에 「증거」로 읽힌 근거 가운데 이 사실이 무너뜨린 것은?
① 카메라 앞의 장면 자체는 있었지만, 그 장면을 찍는 사람이 만들어 놓았으므로 「찍은 사람이 개입하지 않았다」는 신뢰 ② 진흙과 사진이 빛의 흔적으로 남았다는 지표성 자체 ③ 신문에 실렸으므로 캡션이 사라졌다는 점 ④ 사진이 흑백이어서 도상성이 약해졌다는 점
정답 ①. 근거: 5차시 「조작은 AI와 함께 오지 않았다」(연출 사진의 역사)·세 겹의 신뢰. 오답 ②는 「지표성은 남아 있다」는 점을 놓친 것, ③④는 무관한 요소.

**5급 · 영역 ① (A반, 가상 사례, 140자 규칙에 맞춰 줄임)** 한 학생이 주운 깨진 기와를 「미상, 『비 오는 날의 기와』, 2026, 발견된 오브제」라는 캡션과 함께 공지 게시판에 붙였다. 아무도 읽지 않았는데, 교사가 그것을 미술실 전시대로 옮기자 학생들이 멈춰 서서 보기 시작했다. 디키의 제도론과 「맥락의 여섯 장치」로 볼 때 가장 알맞은 판단은?
① 사물은 같았고 바뀐 것은 장소와 전시대라는 제도의 장치이므로, 감상 후보의 지위는 옮겨진 뒤에 생겼다 ② 캡션을 붙인 순간 이미 작품이었으므로 장소는 무관하다 ③ 학생들이 읽지 않은 것은 기와가 아름답지 않기 때문이다 ④ 교사가 옮겼으므로 저자는 교사다
정답 ①.

B반 쌍둥이는 같은 명세로 다른 문장·다른 사례(예: 5급 ③은 「경매 도록의 유물 사진」, 5급 ①은 「온라인 중고 장터에 올린 사물」)를 쓴다.

### 5.9 접근성

- 보기 4개는 `role="radiogroup"`/`role="radio"` 버튼이라 키보드(Tab·방향키·Space)로 고를 수 있다. 「블록 제출」 앞의 확인 창은 브라우저 대화 상자다. Esc는 전체화면 종료로 잡히므로 화면 안에서 따로 쓰지 않는다. 초점 표시는 2px 실선.
- 글자 크기 3단계(18/22/26px), 줄 간격 1.6, 본문 대비 7:1 이상. 고른 보기는 색과 함께 굵은 테두리·「선택됨」 글자로 표시한다(색만으로 구분하지 않는다).
- 남은 시간 표시줄은 `role="status" aria-live="polite"`이고 10·5·1분에만 문구가 바뀐다(매초 낭독을 막기 위해 숫자는 `aria-hidden` 별도 요소). 이미지 대체 텍스트는 §5.6.
- 시간 배수(§3.4)와 이미지 문항 제외(§5.4)는 교사가 학생별로 설정한다.

---

## 6. 사후 통계와 교사 화면

### 6.1 문항 통계(문항을 본 학생만 분모로)

| 지표 | 계산 | 표시 규칙 |
|---|---|---|
| 노출 수 n_i | 그 문항을 받은 학생 수 | 항상. **n_i < 5 회색(해석 보류)**, 판정 표시는 **n_i ≥ 8**에서만 |
| 정답률 p_i | 정답 수 / n_i, Wilson 95% 구간과 함께 | 급별 목표(.85/.75/.60/.50/.40)와 나란히 |
| 급별 기대 범위 | 1급 .75~.95, 2급 .65~.90, 3급 .50~.80, 4급 .40~.70, 5급 .30~.60 | n_i ≥ 8이고 범위 밖 → 「난이도 재검토」(`levelCheck`) 노랑 |
| 변별도 r_pb | 문항 정오와 「총 정답 수에서 그 문항을 뺀 값」의 점이연 상관(교정 상관) | n_i ≥ 8: r_pb < .10 「변별 없음」(`lowDisc`) 노랑, r_pb < 0 「정답 키 확인」(`keyCheck`) 빨강 |
| 보기 분석 | 보기별 선택 비율(상위 절반 학생의 비율 별도) | n_i ≥ 8: 오답 선택률 < 5% 「기능하지 않는 오답」(`deadOption`), 어떤 오답을 상위 절반의 50% 이상이 고름 「정답 키 확인」 빨강 |
| 급 오배정 의심 | (i) p_i가 한 급 위 문항들의 평균보다 낮음, (ii) 한 급 아래 평균보다 높음 | n_i ≥ 8. 「다음 해 급 이동 후보」 목록 |
| 응답 시간 중앙값 | 문항별(선택 시각 `pickMs`의 간격으로 근사, §0) | 권장(40/50/70/90/120초)의 2배 초과 → 「길이 재검토」 |
| 쌍둥이 불균형 | 같은 명세 A·B 문항의 정답률 차 | 둘 다 n ≥ 5이고 차 ≥ .30 → 표시(`twinStats`) |
| 급 단조성 | 급별 평균 정답률이 1 > 2 > 3 > 4 > 5인가 | 어긋나는 급 쌍 표시(`levelMonotonic`) |

원칙: **한 학급 자료로 문항을 삭제하거나 급을 옮기지 않는다.** 표시된 문항은 정답 키와 문두를 재검토하고, 정답 키 오류로 확정된 문항만 §4.6으로 재채점한다. 급 이동은 문항 ID 기준 누적 노출이 30을 넘은 뒤(같은 은행을 5~6학급에 쓰면 3급은 한 해, 1·5급은 두세 해) 다음 해에 한다. 코드: `itemStats`, `ITEM_STAT_MIN_N`, `ITEM_FLAG_MIN_N`, `LEVEL_P_RANGE`, `TWIN_DIFF`.

### 6.2 학생별 표시

| 표시 | 조건 | 코드 |
|---|---|---|
| 경계 ①(점수 경계) | c가 자기 행의 경계(F=5: 18·16·14, F=4: 14, F=1: 9)에서 ±1 | `boundary1`, `SCORE_BOUNDS` |
| 경계 ②(라우팅 경계) | 어느 블록의 정답 수가 3 또는 4(한 문항 차이로 이동이 바뀜) | `boundary2` |
| 경계 ③(미응답 있음) | n < 20 | `boundary3` |
| 경로 민감 | 경계 ②에 해당하는 블록이 2개 이상 | `pathSensitive` |
| 불일치 | 교사 재계산(원응답 + `quizKeys`)의 F·c·점수가 학생 브라우저 가채점(`clientScore`)과 다르거나, seed로 재현한 추출이 `served`와 다름 → 빨강 | `mismatch` |
| 미완료 | n < 10 | `incomplete` |
| 이탈 | 경고 수·잠금 수·재접속 수·짧은 이탈 수(5회 이상 `shortBlur`)·중복 접속(`dup`) | `flagsOf` |
| Wilson 걸침 | c/20의 Wilson 95% 구간이 자기 행의 점수 경계를 걸침 | `wilson` |

표시는 표시일 뿐 점수를 바꾸지 않는다. 교사는 표시된 학생의 원응답을 먼저 열어 문항 오류를 확인한다.

### 6.3 응시 기록표(학생 1인 1장, 이의 답변의 유일한 자료)

문항 20개의 급·영역·문항 ID·선택 보기·정오·선택 시각, 블록별 정답 수와 급 이동, 최종급·총 정답·응답 수·점수·등급, 산출표의 자기 행, 이탈 기록(종류·시각·지속·교사 「오탐」 표시), 잠금·해제·보전 시간, 재접속 기록, 시간 배수. 교사 화면 「결과」 카드에서 펼치고 인쇄(`window.print`)한다. 학생은 공개 후 자기 것만 본다. 코드: `recordSheet`.

### 6.4 신뢰도 보고(「기술 통계」로만, 의사결정에 직접 쓰지 않음)

1. 블록 1(전원 3급)의 급내 KR-20 근사와 평균 정답률. 참고값.
2. 라우팅 일관성: 블록 4 규칙을 한 번 더 적용한 급과 F가 같은 학생 비율(하강으로 F가 낮아진 학생 비율).
3. 경계 민감도: 문항 하나가 뒤집힐 때 점수가 바뀌는 학생 수와 비율.
4. A·B 반 평균 점수·최종급 분포. 차이가 1점 이상이면 「반 불균형 의심」(12~13명씩이라 그 아래는 해석하지 않음).
5. 이동 일관성: 상승한 학생의 다음 블록 정답률이 유지한 학생의 다음 블록 정답률보다 낮은가(급이 실제로 더 어려웠는가).
6. 화면 고정 문구(`RELIABILITY_NOTE`): 「한 학급 25명의 결과로는 문항 난이도를 확정할 수 없다(정답률 표준오차 약 .10, 상관 표준오차 약 ±.2). 이 통계는 다음 해 문항을 고치는 데 쓰며 이 학급의 점수를 바꾸는 근거가 아니다. 5문항 블록 하나의 이동 판정은 오차가 크며(p=.7일 때 정답 수 표준편차 1.02), 등급 판정은 ±1등급 오차를 가질 수 있다.」

코드: `reliabilityStats`.

### 6.5 교사 화면 구성(「쪽지시험」 탭, `src-quiz-teacher.jsx`)

| 카드 | 기능 |
|---|---|
| 설정 | 단계 버튼(닫힘·열기·시작 마감·결과 공개), 은행 상태, 시작 마감(분)·경고 한도, 좌석표 텍스트와 A/B 격자 미리 보기, 학생별 시간 배수·이미지 제외, 문항 비활성화, 전체 일시정지/재개 |
| 문항 은행 | 은행 JSON 파일 읽기 → `validateBank` 결과(오류·경고·칸별 수 표) → 「은행 올리기」(공개부·정답부 분리 저장), 문항 열람표, 문항 검토표 CSV |
| 실시간 | 학생별 상태(미시작/진행/잠김/완료)·블록·답한 수·남은 시간·경고·잠금·짧은 이탈·마지막 심장박동·중복 접속. 「해제」(보전 켬/끔)·「시간 추가」·「세션 초기화」·「재개 허용」 |
| 결과 | 「재계산」(§6.2 표시), 응시 기록표, 문항 오류 정정 → 재계산(§4.6), 「결과 공개」, CSV 4종(`쪽지시험_attempts\|responses\|events\|items_<yymmdd>.csv`, 익명 번호 P01…) |
| 문항 통계 | §6.1 표, 급 단조성, 쌍둥이 비교 |
| 신뢰도 | §6.4 |
| 설계 근거 | 규칙 5문장, 점수표, 급 정의, 시간표, 이 문서로의 안내 |

### 6.6 당일 운영 점검표(교사)

1. 전날: 컴퓨터실 한 대에서 연습 화면까지 실행(전체화면 API·이미지 로드·서버 시각 확인). OS 알림(Windows 집중 지원)·메신저·업데이트 알림을 컴퓨터실 PC에서 일괄로 끈다. 좌석표를 입력·출력한다. 은행을 올리고 「은행 상태」가 일치하는지 본다.
2. 0분: §1의 5문장, 「이탈 3회면 잠기고 선생님이 풀어 준다」, 「문항 오류는 전원 정답 처리」를 말한다.
3. 3분: 연습 시작. 전체화면이 안 되는 자리는 이 단계에서 걸러 자리를 옮긴다(옮기면 반이 바뀔 수 있으므로 좌석표를 갱신).
4. 6분: 「열기」. 15분 안에 전원 시작 확인.
5. 6~46분: 실시간 카드를 보며 순시한다. 빨간 칸(잠금·중복 접속)으로 간다. 「전체 일시정지」는 정전·서버 장애에만 쓴다(개인 타이머가 알아서 끝난다).
6. 46~50분: 전원 「완료」 확인 후 「시작 마감」.
7. 당일 저녁: 재계산 → 문항 통계의 빨간 표시(「정답 키 확인」) → 경계·불일치 학생 → 「결과 공개」.

---

## 7. 잠금·보안 규칙

### 7.1 원칙

시스템은 **기록하고 멈추고 교사를 부를 뿐 점수를 깎지 않는다.** 처벌은 교사가 현장 확인과 학교 규정으로 판단한다. 자동 제출은 두지 않는다(오탐 한 번이 성적이 되는 구조는 「설명 가능한 규칙」과 어긋난다).

### 7.2 감지 이벤트 표(`useLockdown`)

| 신호 | 브라우저 API | 판정 | 기록 `type` |
|---|---|---|---|
| 탭·창 전환 | `document.visibilitychange`(hidden), `window.blur` | 지속 **2초 미만** = 「짧은 이탈」 기록만. **2초 이상 = 경고 1회.** hidden과 blur가 500ms 안에 겹치면 한 건 | `blur_short` / `blur`, `ms` |
| 전체화면 종료 | `fullscreenchange`에서 `document.fullscreenElement == null` | 문항을 가리고 「전체화면으로 돌아가기」 버튼. **10초 안에 복귀하면 기록만, 넘기면 경고 1회** | `fs_exit`, `ms` |
| 개발자 도구·소스 보기 시도 | `keydown`: F12, Ctrl+Shift+I/J/C, Ctrl+U | preventDefault + **경고 1회** | `devtools_key`, `key` |
| 마우스 이탈 | `document.documentElement` `mouseleave` | **기록만**(경고 아님). 2초 안에 blur가 따라오면 그 blur 건에 `mlb: true` | `mouseleave` |
| 우클릭·복사·잘라내기·붙여넣기·드래그 | `contextmenu`, `copy`, `cut`, `paste`, `dragstart` preventDefault | 막고 기록만 | `blocked_action`, `action` |
| 인쇄·저장·찾기 단축키 | Ctrl/⌘+P·S·F·X·C·V, PrintScreen(keyup) | 막고 기록만(습관적 Ctrl+S가 경고가 되지 않게) | `blocked_key`, `key` |
| 창 크기 변화 | `resize`로 뷰포트가 화면의 90% 미만 | 기록 + 교사 화면 「도킹 의심」 | `resize`, `w`, `h` |
| 중복 접속 | 같은 학번의 두 번째 세션이 `hb`(20초 주기)가 60초 안이고 `sess`가 다른 문서를 발견 | 나중 세션 차단(「다른 곳에서 응시 중입니다」), 기록, 교사 화면 표시. 교사가 「세션 초기화」로 풀어 준다 | `dup` |
| 재접속 | 응시 문서가 있는 상태의 페이지 로드 | 기록만(경고 아님). 「이어서 풀기」 버튼(전체화면 재진입) | `reconnect` |
| 연결 끊김 | `navigator.onLine === false` | 제출 버튼을 막고 「연결을 확인해 주세요」(이벤트 기록 없음, §0) | 없음 |

새로고침(F5·Ctrl+R)은 막지 않는다. 재접속 복구가 있으므로 막을 이유가 없고, 화면이 멈춘 학생이 새로고침도 못 하면 경고만 쌓인다. Alt+Tab·Win 키·브라우저 자체 단축키(Ctrl+T 등)는 페이지가 막을 수 없고 그 결과로 오는 blur만 잡힌다. 모든 이벤트에 `block`(블록 번호)·`item`(마지막으로 만진 문항 번호)·`warn`(그 시점 누적 경고)이 붙는다.

### 7.3 경고·조치 단계(경고 누적 기준, `warnLimit` 기본 3)

| 누적 경고 | 학생 화면 | 교사 화면 | 조치 |
|---|---|---|---|
| 1회 | 문항을 가리는 안내 화면 「다른 창으로 이동한 것이 기록되었습니다. 시험 화면에 머물러 주세요.」 + 「돌아가기」 버튼(전체화면 재요청). 타이머 계속 | 노란 점 | 없음 |
| 2회 | 같은 화면 + 「한 번 더 기록되면 시험이 잠깁니다」 | 노란 점 + 시각·종류 | 없음 |
| 3회 | **시험 잠금.** 문항이 가려지고 「시험이 잠겼습니다. 선생님을 불러 주세요.」 **타이머 정지**(`lockedAt` 서버 시각, `status: locked`, `lock.count + 1`) | 빨간 점 | 교사가 교사 화면 「해제」를 누른다. 해제 시 「시간 보전」 체크(기본 켜짐) → 멈춘 초를 `pausedTotalSec`에 더함. 부정으로 판단하면 체크를 끈다(보전 0) |
| 잠금 뒤 | 경고 카운터는 이어진다. 6회에 두 번째 잠금, 9회에 세 번째 잠금 + 교사 화면 「감독 개입 필요」 굵게 | | 그래도 시스템은 제출하지 않는다 |

교사는 학생 화면 앞에서 확인한 뒤 이벤트에 「오탐」을 표시할 수 있다(`v.falsePos`에 이벤트 번호, 기록 자체는 지우지 않음). 응시 기록표에 그대로 나온다.

### 7.4 오탐 방지 규칙

- 「시험 시작」 버튼에서 `requestFullscreen()`이 성공해야 첫 블록을 연다(사용자 제스처 필요). 거부되면 시작할 수 없고 「브라우저가 전체화면을 막았습니다. 선생님께 알려 주세요」.
- 시작 뒤 **첫 30초는 준비 구간**: 이 사이의 blur·전체화면 이탈은 `prep: true`로 기록만 하고 경고로 세지 않는다(전체화면 전환 직후 브라우저 안내 표시줄·초점 이동이 여기서 일어난다).
- 같은 신호가 **1초 안에** 여러 번 오면 한 건으로 병합한다(일부 브라우저는 `fullscreenchange`를 두 번 낸다). 전체화면 이탈과 blur가 함께 오면 하나로 센다.
- 경고 화면·잠금 화면·전체 정지 화면이 떠 있는 동안은 이벤트를 세지 않는다(페이지 안 요소는 blur를 내지 않는다).
- 모든 이벤트에 그 순간의 블록·문항 번호를 함께 저장해 교사가 맥락을 본다.
- 연습 구간에서 학생이 경고 화면을 한 번 보게 해 본시험에서 놀라 창을 닫는 일을 줄인다. 연습 구간의 이벤트는 기록하지 않는다.
- 2초 미만의 「짧은 이탈」이 반복되면(5회 이상) 교사 화면에 「짧은 이탈 반복」을 표시해 1.9초씩 훔쳐보는 행동을 사람이 본다.

### 7.5 재접속·이어 풀기·타이머

- 시작 시각은 「시험 시작」 때 `startedAt: serverTimestamp()`로 기록한 값이고(`fbStore.quizStart`), 클라이언트는 문서를 서버에서 되읽어(`quizReadServer`) 서버 시각과 자기 시계의 차이를 구한다. 20초마다 `hb: serverTimestamp()`를 쓰고 되읽어 재동기화한다(클라이언트 시계 조작 무력화 + 중복 접속 감지를 한 메커니즘으로).
- 남은 시간 = `durationSec × 배수 + pausedTotalSec + extraSec + pausedAccumSec − (서버 현재 시각 − startedAt)`.
- 고른 답은 고를 때마다(300ms 디바운스) `draft`에 저장하고, 블록 제출은 `blocks[k]`에 한 번 쓴다(이후 불변). 재접속하면 `startedAt`·`served`·`draft`·제출된 블록을 읽어 **같은 블록의 같은 자리**에서 이어 푼다. seed가 학번으로 고정돼 있어 문항·순서·보기 배열이 그대로다. 제출한 블록은 다시 열리지 않는다.
- 제한 시간 강제는 Firestore 규칙(§8.3 `quizWithinTime`)이 한다: 진행 필드 쓰기는 `request.time < startedAt + (durationSec × 배수 + pausedTotalSec + extraSec + pausedAccumSec + 60)초`일 때만 허용. 60초는 네트워크 지연분이다. 시간이 지난 뒤에는 마무리 필드(`status`·`finishedAt`·`clientScore`·`events`·`lock`·`warn`·`cur`)만 쓸 수 있다. 규칙에 걸린 쓰기는 「시간이 끝나 저장되지 않았습니다」로 알린다.
- 오프라인 상태에서는 「블록 제출」을 막고 「연결을 확인해 주세요」를 보인다(제출 시각이 서버 시각이어야 하므로). Firestore 오프라인 큐가 초안 쓰기를 보관하되 규칙의 시간 한도는 도착 시각 기준이므로, 오래 끊긴 학생은 교사가 「재개 허용」을 켜 같은 seed로 남은 블록만 다시 풀게 하고 `extraSec`으로 잃은 시간을 준다(사유 `extraReason` 기록).
- PC 고장: 다른 자리로 옮겨 로그인하면 그대로 이어진다. 옮기는 데 든 시간은 교사가 `extraSec`으로 보전한다. 자리를 옮겨도 반(A/B)은 바뀌지 않는다(응시 문서 `v.half`에 고정, 규칙 `quizFixedKeys`).
- 시험 종료 후 접속: 「제출되었습니다」만 보인다. 점수·경로는 교사가 공개한 뒤에 보인다.

### 7.6 은행 공개 시점과 정답 키(암호화를 두지 않은 까닭)

- 은행 공개부(`quizBank/v1`: 문항·보기·`keyHash`·`seedSalt`)는 규칙으로 「교사가 시험을 연 뒤(stage가 closed가 아닐 때)」에만 학생이 읽는다. 시험 전에는 문서 자체를 읽을 수 없다.
- 정답은 공개부에 `keyHash = hash53(문항id + "|" + 보기id + "|" + seedSalt).toString(36)`로만 있다. 보기가 4개뿐이라 네 번 해시하면 알아낼 수 있으므로 **비밀이 아니다.** 「소스를 대충 봐서는 안 보이게」 하는 정도라고 학생·학부모 안내에도 정직하게 적는다. 설계서의 AES 암호화를 두지 않은 까닭은 §0에 있다.
- 채점의 권위는 학생 브라우저에 없다. 학생 화면의 점수는 「가채점」(`clientScore`)이고, **교사 화면이 원응답(문항 ID·고른 보기)과 교사만 읽는 `quizKeys/v1`로 경로·F·c·점수를 다시 계산**한다(`recompute`). 두 값이 다르거나 seed 재현이 `served`와 다르면 「불일치」로 표시한다. 학생이 자기 점수 필드를 고쳐도 소용없고, `result`는 규칙상 쓸 수도 없다.

### 7.7 브라우저가 할 수 없는 것(정직한 기술)

- 휴대전화·종이 메모·옆 사람과의 대화·두 번째 모니터에 초점 없이 띄운 창·화면을 찍는 카메라·가상 머신·원격 데스크톱·브라우저 확장은 감지하지 못한다. 감독으로 막는다.
- 개발자 도구를 여는 학생은 정답을 알아낼 수 있다(keyHash 네 번 계산). 그 시도는 F12·창 크기 변화·blur로 흔적이 남고 점수는 교사 재계산이므로 「점수 조작」은 막지만 「정답을 알고 푸는 것」은 못 막는다.
- 개발자 도구 열림 감지(창 크기 차이)는 신뢰할 수 없어 「의심」 표시만 한다.
- 이 잠금은 「부정행위를 막는 장치」가 아니라 「감독 교사가 볼 곳을 알려 주는 장치」다. 억제의 실체는 감독·좌석 색 분리·학생별 문항 순서·1회 응시·40분 안의 20문항이다. 학교 컴퓨터실이 키오스크 모드를 지원하면 그것이 이 모든 규칙보다 낫다.
- 컴퓨터실 브라우저가 전체화면 API를 막아 두었거나 오래된 버전이면 전날 점검에서 걸러 낸다.

---

## 8. 데이터 구조(Firestore, 구현 확정)

모든 문서는 `fbStore` 규약대로 `{ v: <값>, updatedAt: Date.now() }` 봉투를 쓴다. 응시 문서만 봉투 밖에 서버 시각 필드가 더 있다.

### 8.1 컬렉션과 문서

| 문서 | fbStore 키 | 내용 | 규칙 요약 |
|---|---|---|---|
| `meta/config`의 `v.quiz` | `config` | `stage`(closed/open/ended/published), `openedAtMs`, `startDeadlineSec`(900), `durationSec`(2400), `warnLimit`(3), `paused`·`pausedAtMs`·`pausedAccumSec`, `timeMult`({학번: 1.5}), `noImage`({학번: true}), `disabled`([문항id]), `seatGrid`, `bankVer`, `title` | 로그인 사용자 읽기, 교사만 쓰기(기존 `meta` 규칙) |
| `quizBank/v1` | `quizBank` | `ver`, `seedSalt`, `itemCount`, `practice`(2문항, 정답 포함), `items[]`: `id`, `level`, `area`, `half`, `spec`, `stem`, `options`(4개 `{id, text}`), `keyHash`, `image`(`{src, alt}` 또는 null), `neg` | 읽기: 교사 또는 stage != closed인 로그인 사용자 / 쓰기: 교사 |
| `quizKeys/v1` | `quizKeys` | `ver`, `seedSalt`, `items`({문항id: {answer, expl, src, level, area, half, spec, tags}}), `corrections`([{itemId, allCorrect, reason, at}]) | 교사만 읽기·쓰기 |
| `quiz/{학번}` | `quiz:<학번>` | 봉투 밖: `startedAt`(서버 시각, 불변), `hb`(심장박동), `lockedAt`. `v`(학생): `ver`, `sid`, `half`, `seed`, `bankVer`, `timeMult`, `noImage`, `sess`, `status`(running/locked/done), `cur`, `served`, `draft`, `blocks`, `events`(최대 300), `warn`, `lock`, `finishedAt`, `clientScore`, `client`. `v`(교사만): `pausedTotalSec`, `extraSec`, `extraReason`, `falsePos`, `note`, `resumeOk`, `result` | 본인·교사 읽기 / 학생: 시작 마감 안 생성·제한 시간 안 진행 필드 갱신 / 교사: 무엇이든 |

`result`(교사가 「재계산」·「결과 공개」 때 씀): `path`, `blockCorrect`, `F`, `c`, `n`, `score`, `band`, `flags`, `mismatch`, `keyVer`, `computedAt`, `published`, `publishedAt`, `items`(공개용 사본: 문두·보기(표시 순서)·내 답·정답·정오·해설), `warnCount`, `lockCount`.

### 8.2 seed·추출·채점의 재현 계약

교사 화면과 학생 화면은 같은 순수 함수(`src-quiz-core.mjs`)를 공유하고 `node --test`로 검증한다:

- `deriveSeed(학번, seedSalt) → uint32`
- `drawBlock({ bank, half, seed, level, used, disabled, noImage, k }) → { itemIds, crossHalf, skipped }`
- `scoreAttempt({ blocks, keys, corrections }) → { path, blockCorrect, F, c, n, score, band, answered }`
- `replayServed({ bank, half, seed, path, served, disabled, noImage }) → { ok, expected, mismatches }`

교사 재계산(`recompute`)은 `served`를 신뢰하지 않고 `drawBlock`을 다시 돌려 대조한다. 대조가 어긋나면 「불일치」이고, 점수는 `served`(실제로 본 문항)와 원응답으로 계산한 값을 쓴다.

### 8.3 Firestore 규칙(`firestore.rules`에 들어간 그대로)

기존 보조 함수 `signedIn()`·`uid()`(이메일 접두사)·`isTeacher()`·`isOwner(id)`·`sizeOk()`를 그대로 쓴다.

```
    function quizV(res) {
      return res.data.get('v', {});
    }
    function quizCfg() {
      return get(/databases/$(database)/documents/meta/config).data.get('v', {}).get('quiz', {});
    }
    function quizStage() {
      return quizCfg().get('stage', 'closed');
    }
    // 교사만 쓰는 v 안의 키
    function quizTeacherKeys() {
      return ['pausedTotalSec', 'extraSec', 'extraReason', 'falsePos', 'note', 'resumeOk', 'result'];
    }
    // 시작 뒤 값이 고정되는 v 안의 키
    function quizFixedKeys() {
      return ['sid', 'half', 'seed', 'ver', 'bankVer', 'timeMult', 'noImage'];
    }
    // 열린 상태이고 시작 마감(열린 시각 + startDeadlineSec) 전인가
    function quizStartOpen() {
      let cfg = quizCfg();
      return cfg.get('stage', 'closed') == 'open'
          && request.time.toMillis() < cfg.get('openedAtMs', 0) + cfg.get('startDeadlineSec', 900) * 1000;
    }
    // 서버 시각이 시작 시각 + (제한 시간 × 배수 + 보전 + 추가 + 전체 정지 + 60초 유예) 안인가
    function quizWithinTime(sid) {
      let cfg = quizCfg();
      let b = quizV(resource);
      return request.time.toMillis() < resource.data.startedAt.toMillis()
          + (cfg.get('durationSec', 2400) * cfg.get('timeMult', {}).get(sid, 1)
             + b.get('pausedTotalSec', 0) + b.get('extraSec', 0) + cfg.get('pausedAccumSec', 0) + 60) * 1000;
    }
    // 이미 있던 블록 키는 바뀌지 않는다 (새 블록을 더하는 것만 허용)
    function quizBlocksKept() {
      let b = quizV(resource).get('blocks', {});
      return !quizV(request.resource).get('blocks', {}).diff(b).affectedKeys().hasAny(b.keys());
    }
    function quizServedKept() {
      let b = quizV(resource).get('served', {});
      return !quizV(request.resource).get('served', {}).diff(b).affectedKeys().hasAny(b.keys());
    }
    function quizEventsGrow() {
      return quizV(request.resource).get('events', []).size() >= quizV(resource).get('events', []).size();
    }
    match /quizBank/{docId} {
      allow read: if isTeacher() || (signedIn() && quizStage() != 'closed');
      allow write: if isTeacher();
    }
    match /quizKeys/{docId} {
      allow read, write: if isTeacher();
    }
    match /quiz/{sid} {
      allow read: if isOwner(sid) || isTeacher();
      allow delete: if isTeacher();
      allow create, update: if isTeacher() && sizeOk();
      allow create: if isOwner(sid) && sizeOk()
                    && quizStartOpen()
                    && request.resource.data.startedAt == request.time
                    && !quizV(request.resource).keys().hasAny(quizTeacherKeys())
                    && quizV(request.resource).get('finishedAt', null) == null;
      allow update: if isOwner(sid) && sizeOk()
                    && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['v', 'hb', 'lockedAt', 'updatedAt'])
                    && request.resource.data.startedAt == resource.data.startedAt
                    && quizV(resource).get('finishedAt', null) == null
                    && !quizV(request.resource).diff(quizV(resource)).affectedKeys().hasAny(quizTeacherKeys())
                    && !quizV(request.resource).diff(quizV(resource)).affectedKeys().hasAny(quizFixedKeys())
                    && quizBlocksKept() && quizServedKept() && quizEventsGrow()
                    && (quizWithinTime(sid)
                        || quizV(request.resource).diff(quizV(resource)).affectedKeys()
                             .hasOnly(['status', 'finishedAt', 'clientScore', 'events', 'lock', 'warn', 'cur']));
    }
```

읽는 법:
- `request.resource.data`는 merge 쓰기의 **결과 문서**이므로, 학생이 `draft`나 `events`(arrayUnion)만 보내도 규칙은 결과 전체를 본다.
- `get(meta/config)`는 규칙 평가 한 번에 최대 1회만 부른다(`let cfg`). 요청당 10회 한도에 여유가 있다.
- 학생 create의 `startedAt == request.time`은 클라이언트가 `serverTimestamp()`를 보낼 때만 참이다. 같은 학번이 `quizStart`를 다시 부르면 update가 되고 `startedAt`이 바뀌므로 거부된다(재시작 불가).
- `quizPatch`는 vPatch가 비어 있으면(심장박동) `v`를 아예 보내지 않는다. `v: {}`를 merge로 보내면 `v` 전체가 빈 맵이 된다.
- 잘못된 쓰기는 클라이언트에서 `false`로 돌아오고, 화면은 「시간이 끝나 저장되지 않았습니다」 또는 재시도 안내를 보인다.

### 8.4 클라이언트 상태 기계

`안내(연습) → 시작(전체화면·quizStart·되읽기) → running(block k) → [locked ⇄ running] → done`. 시간 종료는 `running`에서 현재 블록을 `auto: true`로 제출하고 `done`으로 간다. 재접속 시 문서가 있고 `status != done`이면 「이어서 풀기」(전체화면 재진입) 뒤 `running`으로 복귀한다. `stage == published`이고 `result.published`이면 결과 화면.

---

## 9. 위험·한계·대안

### 9.1 이 설계가 틀릴 수 있는 지점

1. **급 사전 배정의 오류.** 「3급」이라 적은 문항이 실제로는 5급만큼 어려울 수 있다. 한 블록에 그런 문항이 둘 있으면 학생은 억울하게 내려간다. 완화: 쌍둥이 작성, 독립 AI 풀이 점검, 동료 맹검, 3급 블록 시간 확인, 경로 민감 표시, 다음 해 누적 통계. 첫해에는 남는 위험이다. 사후에 블록 1 학급 평균 정답률이 .45 아래이거나 .85 위이면 문항 오류 검토를 먼저 열되 산출식은 바꾸지 않는다.
2. **5문항 블록의 라우팅 잡음.** 유지 구간이 「정확히 3」이라 p=.6 학생은 한 블록에서 세 방향으로 거의 고르게 갈리고(§2.3), 시뮬에서 C형의 31%가 D, B형의 21%가 C를 받는다(§4.7). 네 블록으로 되돌릴 기회를 두었지만 마지막 블록의 잡음은 되돌릴 수 없다. 20문항·40분·10점 쪽지시험의 대가이며, 안내 자료에 「±1등급 오차」를 명시한다.
3. **도달했으나 마지막에 밀린 학생.** 「나」(§4.5)처럼 5급까지 오른 뒤 마지막 블록 2/5로 B 7이 되는 경우, 「라」처럼 4급을 두 번 보고도 C가 되는 경우가 있다. 교사 기준이 「도달한 급에서 정답을 유지」를 요구하므로 받아들이되, 이런 학생은 「경로 민감」으로 표시된다. 대안 「최종급은 최고 도달 급 − 1보다 낮을 수 없다」는 시뮬에서 E가 사라지고(전원 3급 출발이라 하한이 2급) 순수 추측이 D 98%를 받으므로 채택하지 않았다.
4. **C·D의 정답률 범위가 넓다.** C는 8~15(40~75%), D는 7~14. 「절반을 조금 넘는」은 전형적 경로(3급 4블록 3/5 = 60%)에서만 정확하다. C·D에 c 조건을 두면 규칙이 두 줄 늘어나므로 두지 않았고, 이 범위를 학부모 자료에 그대로 적는다.
5. **응답 10문항 미만 = 2점의 절벽과 그 위의 완만함.** n ≥ 10이면 빈 문항이 오답으로 들어가 급이 내려가므로 별도 절벽이 없다(§4.5 「자」). 반대로 3·4급에서 10문항만 풀고 40분을 다 쓴 학생이 C 5를 받는 경우(3(4)→4(4)→미시작→미시작 = c 8, F 3)가 이론상 있으나 문항당 4분 이상을 쓴 것이라 실제로는 드물고, 권장 종료 시각 표시로 줄인다.
6. **A·B 반의 난이도 동등성.** 쌍둥이 작성이 유일한 수단이다. 반별 평균 차이가 1점 이상이면 교사가 알게 되지만 점수를 어떻게 고칠지는 학교 규정 사안으로 남긴다(사전 검사 없이 통계적 동등화를 할 수 없다).
7. **[12미창01-01]의 약한 측정.** 선택형으로는 「진로와 연결된 주제 탐색」을 잴 수 없다(§5.1).
8. **정답 키 노출·개발자 도구.** §7.6~7.7. 서버 없는 조건에서 라우팅을 클라이언트가 하는 한 피할 수 없다. 이 구현은 암호화를 두지 않아 설계서보다 「소스 훑어보기」에 조금 더 열려 있으나, 시험 전 읽기 차단과 교사 재계산이라는 실질적 방어는 같다(§0).
9. **급 숨김과 투명성의 긴장.** 「내가 몇 급인지 왜 안 보여 주나」는 이의가 예상된다. 규칙 사전 공개 + 사후 전체 공개로 답하며, 안내문에 미리 적는다. 시간 배분은 블록 번호 기준 권장 종료 시각(§3.2)으로 돕는다. 어느 급에서든 많이 맞히는 것이 항상 유리하므로 급을 알아도 전략이 달라지지 않는다.
10. **여러 학급에 쓸 때의 노출.** 같은 은행을 5~6학급에 며칠에 걸쳐 쓰면 앞 학급이 뒤 학급에 문항을 전한다. 가능하면 같은 날 응시하고, 안 되면 반을 셋으로 늘려 은행을 210으로 간다(제약 초과이므로 두 해에 걸쳐 확장).
11. **이미지 문항의 화면 차이.** 모니터 화질·크기가 달라 2~4급 도판 문항의 공정성이 흔들릴 수 있다. 8% 상한·블록당 1개·텍스트만으로 풀림 규칙으로 줄이지만 없애지는 못한다.
12. **신뢰도 계수를 보고할 수 없다.** 25명·적응형이라 계수 하나를 낼 수 없고 §6.4의 기술 통계가 그 자리를 대신한다. 숨기지 않는 것이 이 설계의 투명성이다.
13. **단일 문서의 크기.** 이벤트 300건·블록 4개·결과 사본 20문항을 한 문서에 담는다. 1MB 한도에는 넉넉하지만 이벤트가 상한에 닿으면 화면 카운터만 늘고 저장은 멈춘다. 교사 화면에는 상한 도달을 표시한다.

### 9.2 대안과 비교

| 대안 | 버린 이유 |
|---|---|
| 전원 동일 고정형 20~25문항 | 교사 기준의 A·B·C가 「어느 단계에 도달했는가」로 쓰여 있어 고정형으로는 서술을 산출식에 대응시킬 수 없다. 옆자리와 같은 문항이라 부정행위 유인이 크다 |
| 문항 단위 CAT(IRT) | 문항 모수가 없고 25명으로 추정도 못 한다. 왜 이 문항이 나왔는지 설명할 수 없다 |
| 문항 단위 계단(1문항 맞으면 상승) | 한 문항 추측 정답 25%가 걸음마다 오판이 된다 |
| 4문항 × 4묶음 | 추측 상승 5.1%, p=.5 학생 상승·하강 각 31%. 40분이면 5문항 묶음이 들어가므로 4문항을 택할 이유가 없다 |
| 5문항 × 5모듈 = 25문항 | 최악 경로 36.7~45분으로 40분을 넘길 수 있고, 5급 15문항을 한 학생이 푼다. 확보 급 S와 10행 산출표는 한 문장 설명이 안 된다 |
| 6블록 × 5문항 | 최악 경로 53분 |
| 3단계 1-3-3 MST | 다섯 급을 「도달」로 표현할 수 없다 |
| 비대칭 라우팅(하강 ≤1) | 위로 편향: E형이 E를 25%만 받고 순수 추측이 D를 14% 받는다 |
| 「가상 블록 5의 급」 | 풀어 보지 않은 급으로 올려 주어 「도달」이 깨진다 |
| 급별 가중 합산 | 학생마다 만점이 달라 공정성 설명이 안 된다 |
| 감점(오답 −0.25) | 「비워 둘까」 불안을 만들고 빈 문항의 라우팅 영향을 설명하기 어렵다 |
| 자동 제출형 보안 | 오탐 한 번이 성적이 된다. 교사가 교실에 있다 |
| 급을 시험 중 표시 | 「내려갔다」는 신호가 불안만 늘린다. 사전 규칙 공개 + 사후 전체 공개로 대신한다 |
| 3지선다 | 추측 상승이 3배 |
| 은행 AES 암호화 + 암구호 | 암구호가 칠판에 적히는 순간 같은 상태가 되고, 정답이 네 번 해시로 드러나는 구조는 그대로다. 운영 단계와 전날 점검만 늘어난다(§0) |
| 블록·이벤트 하위 컬렉션 | 규칙마다 부모 `get()`이 필요하고 교사 구독이 문서 수십 배로 는다. 단일 문서 + 불변·추가 전용 규칙으로 같은 보장을 얻는다(§0) |

### 9.3 다음 해를 위한 것

첫해 자료(25명 × 20응답)는 문항별 정답률·변별도의 첫 보정 자료다. 문항 ID 기준으로 누적해 노출 30을 넘긴 문항부터 급 재배정과 기능하지 않는 오답 교체를 하고, 서너 학급이 쌓이면 급별 기대 정답률 범위를 자료로 다시 정한다. CSV 4종(`csvAttempts`·`csvResponses`·`csvEvents`·`csvItems`)이 그 누적의 단위다.

---

## 10. 참고 문헌(실재 확인된 것만. 미확인은 「확인 필요」)

- Anderson, L. W., & Krathwohl, D. R. (Eds.). (2001). *A taxonomy for learning, teaching, and assessing: A revision of Bloom's taxonomy of educational objectives*. Longman. 급의 인지 수준 다섯 층.
- Ebel, R. L., & Frisbie, D. A. (1991). *Essentials of educational measurement* (5th ed.). Prentice Hall. 변별도 해석 기준.
- Feldman, E. B. (1994). *Practical art criticism*. Prentice Hall. 8차시 「네 단계로 보는 법」의 출처.
- Haladyna, T. M., Downing, S. M., & Rodriguez, M. C. (2002). A review of multiple-choice item-writing guidelines for classroom assessment. *Applied Measurement in Education, 15*(3), 309–334.
- Hendrickson, A. (2007). An NCME instructional module on multistage testing. *Educational Measurement: Issues and Practice, 26*(2), 44–52.
- Livingston, S. A., & Lewis, C. (1995). Estimating the consistency and accuracy of classifications based on test scores. *Journal of Educational Measurement, 32*(2), 179–197. 분류 일관성 개념. 본 설계는 n=25 한계로 계수를 산출하지 않는다.
- Lord, F. M. (1980). *Applications of item response theory to practical testing problems*. Lawrence Erlbaum.
- Luecht, R. M., & Nungester, R. J. (1998). Some practical examples of computer-adaptive sequential testing. *Journal of Educational Measurement, 35*(3), 229–249.
- Rodriguez, M. C. (2005). Three options are optimal for multiple-choice items: A meta-analysis of 80 years of research. *Educational Measurement: Issues and Practice, 24*(2), 3–13.
- Wainer, H. (Ed.). (2000). *Computerized adaptive testing: A primer* (2nd ed.). Lawrence Erlbaum.
- Weiss, D. J. (1982). Improving measurement quality and efficiency with adaptive testing. *Applied Psychological Measurement, 6*(4), 473–492.
- Wilson, E. B. (1927). Probable inference, the law of succession, and statistical inference. *Journal of the American Statistical Association, 22*(158), 209–212. Wilson 구간.
- Yan, D., von Davier, A. A., & Lewis, C. (Eds.). (2014). *Computerized multistage testing: Theory and applications*. CRC Press. 정답 수 라우팅 MST.
- Zenisky, A., Hambleton, R. K., & Luecht, R. M. (2010). Multistage testing: Issues, designs, and research. In W. J. van der Linden & C. A. W. Glas (Eds.), *Elements of adaptive testing* (pp. 355–372). Springer. 쪽수 확인 필요.
- Ortner, T. M., & Caspers, J. (2011). Consequences of test anxiety on adaptive versus fixed item testing. *European Journal of Psychological Assessment, 27*(3), 157–163. 급 숨김의 근거. 권·호·쪽 확인 필요.
- WHATWG. *Fullscreen API Living Standard*; W3C. *Page Visibility Level 2*. 브라우저 API 규격. 판 번호·연도 확인 필요.
- Google. *Firebase Security Rules* 문서(`request.time`, `Timestamp.toMillis()`, `Map.get()`, `MapDiff.affectedKeys()`, `get()` 호출 한도). 온라인 문서, 열람 시점 확인 필요.
- 교육부 (2022). *미술과 교육과정*(2022 개정). 성취기준 [12미01-02], [12미03-01], [12미감01-01], [12미창01-01].

설계 단계의 검산 스크립트(`final_sim.py`: 이항 확률, 25경로 시간, c 범위 열거, 프로파일별 몬테카를로 20,000회 / `final_sim2.py`: 최종급 규칙 대안 비교)는 저장소에 넣지 않았다. 그 결과 가운데 규칙으로 굳어진 것(경계 사례 11개, 「5급 최종급이면 c ≥ 14」)은 `src-quiz-core.test.mjs`가 다시 확인한다.
